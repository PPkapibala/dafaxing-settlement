# 大发行模式打款分账系统 — 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| **文档版本** | V1.2 |
| **创建日期** | 2026-03-25 |
| **产品名称** | 大发行模式打款分账系统 |
| **文档状态** | 三稿（评审修正：设计矛盾修复 + 边界场景补充 + MVP 精简） |

---

## 一、项目概述

### 1.1 背景与目标

公司在大发行模式下，需要与多个合作方进行复杂的收入结算。

**核心目标**：构建一个自动化的财务分账平台，支持从游戏收入入账 → 自动分账计算 → 提款申请 → 双重审核 → 自动打款的全链路闭环，提升结算效率与准确性。

### 1.2 产品定位

面向公司内部财务团队、业务项目组及外部合作方的 **B端财务结算管理平台**，类似支付宝提现模式，合作方通过虚拟账户查看余额并发起提款。

### 1.3 核心价值

| 价值维度 | 说明 |
|---|---|
| 效率提升 | 分账规则自动执行，消除人工计算误差，减少结算周期 |
| 资金安全 | 双重审批机制 + 资金池管控，确保每笔打款有据可查 |
| 透明可控 | 合作方自助查询收入明细与分成结果，减少沟通成本 |
| 灵活扩展 | 预留阶梯式分成等复杂模式能力，适配未来业务变化 |

---

## 二、用户角色与权限

### 2.1 角色定义

| 角色 | 描述 | 主要功能权限 |
|---|---|---|
| **合作方（CP/联运方）** | 外部合作伙伴 | 查看收入/分成明细、查看待结算金额与可提额、查看本方关联分成规则参数（只读）、发起提款申请、管理银行账户信息 |
| **业务负责人（业务项目组）** | 内部负责游戏运营/商务的团队 | 查看**所负责合作方**的数据看板、审核所负责合作方的提款申请（一审）、管理所负责合作方信息 |
| **财务人员** | 内部财务团队，拥有**最高业务权限** | 管理分成规则（创建/修改/停用）、审核提款申请（终审）、资金池管理、全平台数据看板 |
| **系统管理员** | 平台运维管理 | 用户权限管理、系统配置、日志审计 |

### 2.2 权限矩阵

| 功能模块 | 合作方 | 业务项目组 | 财务人员 | 系统管理员 |
|---|---|---|---|---|
| 数据看板-合作方视图 | ✅ 仅本方数据 | ❌ | ❌ | ❌ |
| 数据看板-公司视图 | ❌ | ✅ 仅所负责合作方 | ✅ 全平台数据 | ✅ 查看 |
| 提款申请-发起 | ✅ | ❌ | ❌ | ❌ |
| 提款申请-业务审核（一审） | ❌ | ✅ | ❌ | ❌ |
| 提款申请-财务终审（二审） | ❌ | ❌ | ✅ | ❌ |
| 分成规则-创建/修改/停用 | ❌ | ❌ | ✅ | ❌ |
| 分成规则-查看 | ✅ 仅本方关联规则（比例、结算周期等参数只读） | ✅ | ✅ | ✅ |
| 合作方入驻-资质审核 | ❌ | ✅ | ✅ | ❌ |
| 用户与权限管理 | ❌ | ❌ | ❌ | ✅ |

---

## 三、功能需求（EARS 语法）

> 使用 EARS（Easy Approach to Requirements Syntax）语法撰写，确保每条需求主语为"系统"、可验证、可测试。参见附录 EARS 语法速查。

### 3.1 分账计算引擎

- **REQ-F-001**：当浑天仪 T+1 数据同步完成时，系统应依据各合作方每款游戏的生效分成规则，自动计算各方应得分成金额并写入对应虚拟账户。
- **REQ-F-002**：系统应支持（收入数据 − 费用扣除项）× 分成比例"两种分成计算公式。
- **REQ-F-003**：系统应按渠道来源、游戏产品、时间区间三个维度归集收入数据，并逐游戏匹配对应分成规则进行计算。
- **REQ-F-004**：系统应在底层数据模型中预留阶梯式分成（根据收入规模浮动比例）的扩展能力。
- **REQ-F-005**：当 T+1 分账计算过程中，某"合作方 + 游戏产品"组合无法匹配到任何生效的分成规则时，系统应跳过该组合的计算，将其标记为"规则缺失"，并告警通知财务人员，附带缺失的合作方名称、游戏名称和流水日期。
- **REQ-F-006**：当分账计算结果为负数（如费用扣除项大于收入）时，系统应将该笔分成金额记录为 0，标记"计算异常-负值"并告警通知财务人员审核，不自动写入虚拟账户。

### 3.2 分成规则管理

- **REQ-F-101**：系统应支持"从已有规则复制"功能，创建新规则时可选择一条已有规则作为基础，复制其分成结构与参数并允许修改，快速生成新规则。
- **REQ-F-102**：当财务角色创建、修改或停用分成规则时，系统应自动触发审批流程（业务负责人审批 → 财务负责人终审）；规则的创建/修改发起人不得担任同一审批流的终审角色（回避机制）。
- **REQ-F-103**：当财务角色修改规则参数（比例、扣除项、结算周期等）时，系统应自动创建新版本申请，审批通过后旧版本冻结为"已失效"且不可篡改。
- **REQ-F-104**：当系统执行 T+1 分账计算时，系统应根据流水日期匹配当时生效的规则版本进行计算，而非使用规则最新版本。
- **REQ-F-105**：系统应仅允许财务角色发起分成规则的创建、修改和停用操作，修改频次不受限制。
- **REQ-F-106**：系统应将每条分成计算结果关联到具体的"规则ID + 版本ID"，确保审计时可精确还原计算依据。
- **REQ-F-107**：系统应在规则详情页展示完整版本时间线（版本号、变更摘要、修改人、审批状态、生效/失效时间），点击版本可查看该版本完整参数快照。

### 3.3 提款与打款

- **REQ-F-201**：当合作方提交提款申请时，系统应校验：①提款金额 ≤ 可提额；②提款金额 ≤ 单笔提款上限；③当日已提款累计 + 本次金额 ≤ 每日提款上限。任一校验不通过时拒绝提交并提示具体原因。
- **REQ-F-212**：当合作方提款申请提交成功时，系统应立即将对应金额从"可提额"转入"冻结金额"，确保该金额不可被其他提款申请重复占用。
- **REQ-F-216**：当同一合作方并发提交多笔提款申请时，系统应通过乐观锁机制保证可提额扣减的原子性；若冻结操作因版本冲突失败，系统应拒绝提交并提示"可提额已变动，请刷新后重试"。
- **REQ-F-213**：当提款申请被驳回或退票时，系统应立即将冻结金额释放回"可提额"。
- **REQ-F-214**：当合作方登录时，系统应在账户总览中分别展示待结算金额、可提额、冻结金额及冻结明细（每笔审批中/排队中/打款中的申请单号、金额和状态）。
- **REQ-F-202**：当提款申请提交成功时，系统应自动创建审批单，并推送通知给对应的业务负责人和财务终审人。
- **REQ-F-203**：当提款申请通过双重审批（业务一审 + 财务终审）时，系统应通过银企直联接口向前置机发起转账指令执行自动打款。
- **REQ-F-204**：当打款成功时，系统应自动扣减合作方待结算金额（从冻结金额中扣除）、标记提款记录为"成功"，并通知合作方。
- **REQ-F-205**：当打款失败时，系统应将状态标记为"待人工处理"，冻结金额保持不释放；财务人员人工核实后可选择"重新发起打款"或"释放冻结（退回可提额）"，操作结果记录审计日志。**财务选择"释放冻结"前，必须先确认打款金额已退回公司资金池（见 REQ-F-220）；未确认回账前，系统禁止解冻。**
- **REQ-F-220**：当打款失败或退票后，系统不得自动释放合作方冻结金额，必须完成资金回账确认后方可解冻，确认流程分两个路径：
  - **自动确认（优先）**：系统应在打款失败/退票后，通过银行流水查询接口（300001）定时检测是否存在与失败打款金额、摘要匹配的入账记录；检测到匹配记录后，系统自动标记"疑似已回账"并推送通知财务复核；财务确认后系统解冻冻结金额，释放回合作方可提额。
  - **人工兜底（自动检测失败或超时时）**：财务人员登录银行网银手动核查流水，确认资金已回账后在系统手动勾选"已确认资金回账"并上传流水截图作为凭证，系统方可解冻。
  - 若财务确认资金**未回账**（仍在银行处理中），系统应保持冻结状态，记录"待资金回账"备注，并禁止合作方重新发起该笔对应金额的提款申请，直至回账确认完成。
- **REQ-F-206**：当银行返回退票通知时，系统应自动将提款状态更新为"已退票"，释放冻结金额（回到可提额），并通知财务人员和合作方。
- **REQ-F-217**：系统应识别并记录退票原因分类（账户名不匹配 / 账户不存在 / 账户已注销 / 余额不足 / 其他），通知内容中应包含退票原因，合作方通知中应给出对应的处理建议（如：账户名不匹配 → 提示修改收款账户）。
- **REQ-F-218**：系统应支持通过银企直联接口轮询查询退票状态（接口 500001）；若银行支持退票主动推送回调（接口 500002），系统应优先注册回调接收，以减少轮询压力。（⚠️ 待沟通：银行退票回盘时效及是否支持推送，见银企接口对接待沟通事项 4.1 / 4.2）
- **REQ-F-219**：当提款申请状态为"已退票"时，财务人员应可对该申请发起"修改收款账户并重新打款"操作；系统应校验新账户已通过审核，并以原申请单号为基础创建重试记录，保持完整审计链路。
- **REQ-F-207**：当前置机通信异常时，系统应自动重试（最多 3 次），重试均失败后生成付款指令文件供财务手动操作。
- **REQ-F-208**：当打款成功时，系统应自动通过银企直联接口下载电子回单，并关联到对应提款申请单作为打款凭证。
- **REQ-F-209**：系统应在每日打款前通过银企直联查询资金池主账户余额，当余额不足以覆盖当日待打款总额时，系统应暂停打款排队并告警通知财务。
- **REQ-F-210**：当资金池余额不足时，系统应按打款队列的审批通过时间先后顺序执行部分打款（先审先付），余额耗尽后剩余申请保持"排队中"状态，待资金补充后自动恢复执行。
- **REQ-F-211**：系统应支持为每个合作方配置提款限额参数（单笔上限、每日累计上限），由财务角色统一设置和调整。
- **REQ-F-215**：当提款申请处于"待一审"状态时，合作方可主动撤回该申请；系统应立即释放冻结金额（冻结→可提额），并将申请状态标记为"已撤回"。已进入"待终审"及之后状态的申请不可撤回。

### 3.4 数据看板与权限

- **REQ-F-301**：系统应每日凌晨同步浑天仪前一日数据（T+1），平台展示数据与浑天仪保持一致，对账无差异。
- **REQ-F-302**：当合作方用户登录时，系统应仅展示与该合作方关联的收入、分成、提款及账户数据。
- **REQ-F-303**：当业务负责人登录时，系统应仅展示其所负责合作方的数据，不展示其他合作方数据。
- **REQ-F-304**：当财务人员登录时，系统应展示全平台所有合作方的汇总数据及明细数据。

### 3.5 合作方入驻与资质

- **REQ-F-401**：当合作方提交入驻申请时，系统应要求上传营业执照、法人身份证、银行账户信息及联系人信息。
- **REQ-F-402**：当合作方入驻申请提交时，系统应触发双重审核流程（业务项目组初审 → 财务复审）。
- **REQ-F-403**：当合作方资质文件（营业执照等）距到期不足 30 天时，系统应自动发送更新提醒。

### 3.6 银行账户与资金安全

- **REQ-F-501**：当合作方新增或变更银行账户时，系统应要求提交审核，财务审核通过后方可用于提款。
- **REQ-F-502**：当合作方存在"打款中"状态的提款申请时，系统应禁止变更该笔提款使用的银行账户。
- **REQ-F-503**：系统应采用公司自有银行账户作为资金池，合作方在平台上的资金体现为待结算金额（虚拟账户）。
- **REQ-F-504**：MVP 阶段系统应使用单一创梦公司银行账户作为付款方发起所有转账；付款账户号应通过系统配置项管理，不得硬编码于业务逻辑中。
- **REQ-F-505**：（V2 迭代）系统应支持配置多个付款账户，财务人员可在打款时指定使用的付款账户，以满足多主体或业务线资金隔离需求；V1 阶段预留该扩展点，数据模型中付款账户字段应与提款申请单关联存储。

### 3.7 合作方终止与退出

- **REQ-F-701**：当财务或业务负责人发起合作方终止操作时，系统应校验该合作方名下是否存在未完结的提款申请（审批中/排队中/打款中），若存在则阻止终止并提示先处理未完结申请。
- **REQ-F-702**：当合作方终止且待结算金额 > 0 时，系统应要求财务确认剩余金额的处理方案（最终提款清算 / 挂账冻结 / 合同约定处理），并记录处理结果。
- **REQ-F-703**：当合作方状态变更为"已终止"后，系统应停用该合作方名下所有生效中的分成规则、冻结其平台账号登录权限、并将关联的合作关系标记为"已终止"。
- **REQ-F-704**：系统应保留已终止合作方的全部历史数据（收入、分成、提款记录等），供财务和审计人员按权限查阅。

### 3.8 合作方-业务负责人归属

- **REQ-F-801**：系统应支持为每个合作方指定一个业务负责人，建立归属绑定关系。
- **REQ-F-802**：当合作方归属关系变更时，系统应记录变更历史（原负责人、新负责人、变更时间、变更原因），且"待一审"的提款申请自动重新路由至新负责人。
- **REQ-F-803**：系统应基于归属关系自动控制业务负责人的数据可见范围和提款审批权限。

---

## 四、功能模块详细设计

### 4.0 核心实体关系：合作方与游戏产品

> 一个合作方可合作多款游戏，每款游戏可以有独立的分成规则、独立的收入数据和分成计算结果。

#### 合作方-游戏产品关联数据结构

| 字段 | 类型 | 说明 |
|---|---|---|
| 关联ID | String | 系统自动生成 |
| 合作方 | Reference | 关联的合作方 |
| 游戏产品 | Reference | 关联的游戏产品 |
| 合作模式 | Enum | 独代 / 联运 / 其他 |
| 合作生效时间 | DateTime | 合作开始日期 |
| 合作失效时间 | DateTime | 合作到期日期（可为空，表示长期合作） |
| 状态 | Enum | 合作中 / 已终止 |

#### 关系说明

- **合作方 1 : N 游戏产品**：同一合作方可合作多款游戏，每款游戏的收入和分成独立核算
- **分成规则按游戏维度配置**：一条分成规则需明确绑定到具体的"合作方 + 游戏产品"组合，不同游戏可适用不同比例
- **收入数据按游戏归集**：浑天仪同步的流水数据按游戏产品维度归集，分账计算时逐游戏匹配对应规则
- **合作方看板按游戏拆分**：合作方可查看每款游戏的收入明细和分成结果，也可查看汇总数据

### 4.1 模块一：数据看板

> 数据更新策略：T+1 更新，与浑天仪数据保持一致。

#### 4.1.1 合作方数据视图

面向合作方用户，仅展示与其关联的数据。

| 子模块 | 功能描述 | 交互说明 |
|---|---|---|
| **我的累计收入** | 按游戏、按日维度展示总收入 | 支持按游戏筛选、按日期范围筛选，折线图 + 数据表格双视图 |
| **我的账户总览** | 展示待结算金额、可提额、冻结金额及其关系 | 顶部三张卡片（见下方账户总览设计） + 下方按游戏的分布列表 |
| **我的分成明细** | 每笔收入对应的分成规则、版本号、比例及计算出的分成金额 | 支持按时间、游戏、规则筛选，支持导出；点击规则可查看当时生效的版本参数 |
| **我的提款记录** | 所有提款申请的状态、金额、申请与到账时间 | 列表展示，支持状态筛选（审批中/排队中/打款中/已完成/已驳回/已撤回/已退票） |
| **我的账户信息** | 已绑定的银行账户状态及历史变更记录 | 银行账户列表，支持新增/编辑/停用 |

**合作方"我的账户总览"设计**

```
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  待结算金额      │  │   可提额  ✅    │  │  冻结金额  🔒   │
│  ¥ 500,000.00   │  │  ¥ 320,000.00   │  │  ¥ 180,000.00   │
│                 │  │  [立即提款]      │  │  查看冻结明细 →  │
└─────────────────┘  └─────────────────┘  └─────────────────┘

已结算金额（历史累计到账）：¥ 1,200,000.00

─── 冻结明细 ───────────────────────────────────
申请单号    金额          状态      提交时间
WD-2026001  ¥ 100,000.00  审批中    2026-03-24
WD-2026002  ¥ 80,000.00   打款中    2026-03-22
─────────────────────────────────────────────
```

- **待结算金额**：合作方在平台上的虚拟账户总余额，= 可提额 + 冻结金额
- **可提额**：当前实际可以发起提款的最大金额（"立即提款"按钮的操作基准）
- **冻结金额**：已提交但未到账的提款申请总额，点击可展开冻结明细列表，展示每笔的单号、金额和当前状态（审批中/排队中/打款中）
- **已结算金额**：历史累计到账金额，展示在下方作为参考

#### 4.1.2 公司财务/业务视图

面向内部财务人员及业务项目组。

| 子模块 | 功能描述 | 交互说明 |
|---|---|---|
| **全平台收入大盘** | 所有合作方收入汇总 | 支持最小按日查看趋势，折线图/柱状图 + 汇总数字 |
| **各合作方分成汇总** | 列表展示每位合作方的待结算金额、可提额、冻结金额、已结算金额 | 支持钻取查看单个合作方下各游戏的分成明细 |
| **资金账户总览** | 平台主资金账户余额、近日出入金流水及预估资金需求 | 余额卡片 + 出入金流水时间线 + 预估资金需求图表 |
| **待处理提款申请** | 集中展示所有待复核的提款申请单 | 按状态分 Tab，支持批量操作 |
| **规则生效监控** | 监控各分成规则当前生效版本的调用情况与计算总额 | 规则列表（含当前版本号）+ 关联计算金额汇总 + 版本变更历史 |

### 4.2 模块二：分成规则管理

#### 4.2.1 核心数据指标

| 指标 | 定义 |
|---|---|
| **收入数据** | 合作游戏产生的总流水收入，需区分渠道来源、游戏产品、时间区间 |
| **分成金额** | 单次 T+1 分账计算的结果，即某日/某周期内，根据生效规则计算出的合作方应得金额 |
| **累计分成金额** | 合作方历史所有分成金额的总和（自入驻以来全部分账计算结果的累加） |
| **待结算金额** | 合作方虚拟账户当前总余额 = 累计分成金额 − 已结算金额 |
| **可提额** | 待结算金额中可发起提款的部分 = 待结算金额 − 冻结金额 |
| **冻结金额** | 已发起提款申请但尚未到账的部分（含审批中、排队中、打款中），不可重复提款 |
| **已结算金额** | 合作方历史成功提款（已到账）的累计总额 |

**金额关系公式**

```
累计分成金额 = Σ 每次 T+1 分账计算产生的分成金额
待结算金额   = 累计分成金额 − 已结算金额
可提额       = 待结算金额 − 冻结金额
冻结金额     = Σ（审批中 + 排队中 + 打款中）的提款申请金额
```

**金额流转全景**

```
收入数据 ──T+1分账──→ 分成金额（累加到累计分成金额）
                          ↓
                     待结算金额（虚拟账户余额）
                      ┌─────┴─────┐
                   可提额      冻结金额
                      │           │
                  发起提款 ──→ 冻结（可提额→冻结金额）
                                  │
                    ┌─────┬───────┼───────┐
                  驳回   退票   打款成功   打款失败
                    │     │      │         │
                释放冻结 释放冻结 已结算金额 保持冻结
               （→可提）（→可提）（扣减待结算）（待人工：重打/释放）
```

#### 4.2.2 分成计算公式

**当前模式（百分比分成）**：

```
分成金额 = 收入数据 × 分成比例
```

或带扣除项模式：

```
分成金额 = (收入数据 - 费用扣除项) × 分成比例
```

> **扩展预留**：系统底层需预留阶梯式分成能力（根据收入规模浮动比例），当前版本暂不实现但需设计好数据模型。

#### 4.2.3 规则数据结构

> 采用"主体 + 版本"两层模型：规则主体保持唯一标识不变，每次修改生成新版本快照，历史版本冻结不可篡改。

**规则主体（Rule）**

| 字段 | 类型 | 说明 |
|---|---|---|
| 规则ID | String | 系统自动生成，唯一标识，贯穿所有版本 |
| 规则名称 | String | 规则的业务名称 |
| 适用合作方 | Reference | 关联的合作方（单选，一条规则绑定一个合作方） |
| 适用游戏产品 | Reference | 关联的游戏产品（单选，一条规则绑定一款游戏） |
| 当前生效版本 | Reference | 指向当前正在生效的版本记录 |
| 状态 | Enum | 草稿/生效中/已停用 |
| 创建人 | Reference | 首次创建该规则的用户 |
| 创建时间 | DateTime | 首次创建时间 |

**规则版本（RuleVersion）**

| 字段 | 类型 | 说明 |
|---|---|---|
| 版本ID | String | 系统自动生成，唯一标识 |
| 所属规则 | Reference | 关联的规则主体 |
| 版本号 | Integer | 自增版本号（v1, v2, v3...） |
| 分成模式 | Enum | 百分比分成 / 阶梯分成（预留） |
| 分成比例 | Decimal | 如 0.3 代表 30% |
| 费用扣除项 | JSON | 可选，扣除项目及金额/比例（待补充） |
| 结算周期 | Enum | 日结/周结/月结 |
| 生效时间 | DateTime | 该版本开始生效的时间 |
| 失效时间 | DateTime | 下一版本生效时自动填入，或手动设置 |
| 版本状态 | Enum | 待审批/已生效/已失效/已驳回 |
| 变更说明 | String | 本次修改的原因/备注 |
| 修改人 | Reference | 创建该版本的用户 |
| 修改时间 | DateTime | 该版本创建时间 |
| 审批记录 | Object | 业务审批人+意见、财务审批人+意见 |

**版本机制说明**

- **修改即新建版本**：每次修改参数（比例、扣除项、结算周期等）自动创建新版本，旧版本冻结为"已失效"，数据不可篡改
- **按时间匹配版本**：T+1 分账计算时，根据流水日期匹配当时生效的版本进行计算（而非最新版本）
- **分成明细绑定版本**：每条分成计算结果关联到"规则ID + 版本ID"，审计时精确还原当时的计算依据
- **版本时间线**：规则详情页以时间线形式展示完整版本历史，点击可查看该版本参数快照（MVP 不含跨版本 diff 对比，如需对比可人工查看两个快照）
- **1:1 绑定**：一条规则严格对应一个"合作方 + 游戏产品"组合；若同一合作方有多款游戏，需分别创建独立规则

#### 4.2.4 规则管理功能

| 功能 | 说明 |
|---|---|
| **规则复制** | 创建新规则时可选择"从已有规则复制"，系统复制其分成结构与参数（合作方、游戏需重新选择），修改后提交审批。降低重复配置成本 |
| **创建规则** | 仅财务角色可操作；从空白创建或从已有规则复制 → 填写参数 → 提交审批；审批通过后自动生成 v1 版本 |
| **修改规则** | 仅财务角色可操作；修改提交后系统创建新版本（v2, v3...），旧版本自动冻结；新版本需重新走审批流程；修改频次不受限制 |
| **停用规则** | 仅财务角色可操作；停用需审批确认；停用后当前生效版本标记为"已失效" |
| **规则生效逻辑** | 依据合同约定的时间，按生效时间对应的流水生效。新版本生效后不影响旧版本时间范围内已计算的分成 |
| **版本历史** | 规则详情页展示完整版本时间线（版本号、参数变更摘要、修改人、审批状态、生效/失效时间）；点击任一版本可查看完整参数快照 |

#### 4.2.5 规则变更审批流

```
财务创建/修改规则（生成新版本，状态：待审批）
    ↓
系统校验：发起人 ≠ 终审人（回避机制）
    ↓
业务负责人审批
    ↓ 通过              ↓ 驳回
财务负责人终审       版本标记为"已驳回"，退回修改
（不可与发起人为同一人）
    ↓ 通过              ↓ 驳回
新版本生效           版本标记为"已驳回"，退回修改
（旧版本自动失效）
```

> **回避机制**：若财务团队仅一人，终审自动升级至上级主管或系统管理员审批，确保创建人与终审人不为同一自然人。

### 4.3 模块三：提款管理

#### 4.3.1 提款流程

```
合作方发起提款申请
    ↓
系统校验（金额 ≤ 可提额 且 ≤ 单笔上限 且 当日累计未超限）
    ↓ 通过                      ↓ 不通过
冻结对应金额（可提额→冻结金额）  拒绝提交，提示具体原因
    ↓
自动创建审批单 + 推送通知（状态：待一审）
    ↓                       ↓ 合作方主动撤回
业务项目组负责人审核（一审）  释放冻结 → 标记"已撤回"
    ↓ 通过              ↓ 驳回
财务终审（二审）     释放冻结（冻结→可提额）+ 通知合作方（附驳回原因）
    ↓ 通过              ↓ 驳回
进入打款队列         释放冻结（冻结→可提额）+ 通知合作方（附驳回原因）
    ↓
资金池余额校验
    ↓ 充足                      ↓ 不足
通过银企直联发起转账         进入"排队中"状态（先审先付）
    ↓                          待资金补充后自动恢复
银行执行转账
    ↓ 成功              ↓ 失败                  ↓ 退票
扣减待结算金额       标记"待人工处理"         标记"已退票"
（冻结→已结算）      冻结金额保持不释放       释放冻结（冻结→可提额）
+ 下载电子回单归档   财务可选：重打 / 释放    + 通知财务 + 合作方
+ 标记"成功"         + 通知财务人员
+ 通知合作方
```

#### 4.3.2 提款申请字段

| 字段 | 类型 | 说明 |
|---|---|---|
| 申请单号 | String | 系统自动生成 |
| 合作方 | Reference | 发起申请的合作方 |
| 提款金额 | Decimal | 必须 ≤ 可提额，且 ≤ 单笔上限 |
| 收款银行账户 | Reference | 合作方已绑定的银行账户 |
| 申请时间 | DateTime | 提交时间 |
| 申请备注 | String | 可选备注说明 |
| 审批状态 | Enum | 待一审/已撤回/待终审/已通过/排队中/打款中/已完成/打款失败/已退票/已驳回 |
| 一审结果 | Object | 审核人、时间、结果、意见 |
| 终审结果 | Object | 审核人、时间、结果、意见 |
| 打款时间 | DateTime | 实际打款完成时间 |
| 打款流水号 | String | 银行返回的交易流水号 |
| 电子回单 | File | 银企直联自动回盘的电子回单（打款凭证） |

#### 4.3.3 状态流转

```
                    合作方撤回
                       ↓
待一审 ─────→ 已撤回（释放冻结）
  │
  ↓
一审通过 → 待终审 → 终审通过 → 排队中 → 打款中 → 已完成
  ↓           ↓                  ↑         ↓
一审驳回    终审驳回          资金补充    打款失败        已退票
  ↓           ↓              后恢复        ↓               ↓
已驳回      已驳回                      待人工处理      释放冻结
（释放冻结）（释放冻结）             （冻结不释放，     （冻结→可提额）
                                     财务决定重打或释放）
```

#### 4.3.4 消息通知

| 触发事件 | 通知对象 | 通知方式 |
|---|---|---|
| 提款申请提交 | 业务项目组负责人 | 站内信 + 消息推送 |
| 一审通过 | 财务终审人 | 站内信 + 消息推送 |
| 审核驳回 | 合作方申请人 | 站内信 + 消息推送 |
| 打款成功 | 合作方申请人 | 站内信 + 消息推送 |
| 打款失败 | 财务人员 | 站内信 + 消息推送 |
| 银行退票 | 财务人员 + 合作方申请人 | 站内信 + 消息推送 |
| 资金池余额不足 | 财务人员 | 站内信 + 消息推送（告警） |
| 打款排队中 | 合作方申请人 | 站内信（告知审批已通过，因资金调度排队中，预计处理时间） |
| 排队恢复打款 | 合作方申请人 | 站内信（排队已结束，正在执行打款） |

#### 4.3.6 提款限额配置

> 为控制单日资金流出规模和单个合作方集中提款风险，平台应支持多维度限额管控。

**限额参数（由财务角色配置）**

| 参数 | 维度 | 说明 |
|---|---|---|
| 单笔提款上限 | 合作方级别 | 单次提款申请的最大金额，可按合作方单独设置，未设置则使用平台默认值 |
| 每日提款上限 | 合作方级别 | 单个合作方每自然日累计提款金额上限，可按合作方单独设置 |
| 平台每日打款上限 | 平台级别 | 全平台单日累计打款总额上限，防止异常批量提款导致资金池被抽空 |

**限额校验逻辑**

- 合作方提交提款申请时，系统依次校验：可提额 → 单笔上限 → 当日累计上限
- 任一不通过即拒绝，并在提示中明确是哪项限额不足（如"超过每日提款上限，今日剩余可提额为 XX 元"）
- 财务可随时调整限额参数，调整即时生效
- 合作方在"发起提款"页面可看到当前各项限额及已使用额度

#### 4.3.7 资金池不足处理机制

> 当资金池余额不足以覆盖已审批通过的打款请求时，需有明确的排队和恢复机制。

**处理流程**

```
每日打款执行前
    ↓
查询资金池余额（银企直联）
    ↓
按"审批通过时间"升序排列待打款队列
    ↓
逐笔校验余额是否足够
    ↓ 足够              ↓ 不足
执行打款             该笔及之后进入"排队中"
    ↓                    ↓
继续下一笔          告警通知财务（附排队笔数、排队总金额）
                         ↓
                    财务补充资金池
                         ↓
                    系统定时检测余额恢复（每小时）
                         ↓
                    余额充足后自动按排队顺序继续打款
```

**关键规则**

| 规则 | 说明 |
|---|---|
| **先审先付** | 排队按审批通过时间先后执行，不按金额大小 |
| **冻结不释放** | 排队中的申请仍处于冻结状态，合作方不可用该部分金额重复申请 |
| **合作方可见** | 合作方可在提款记录中看到"排队中"状态，但不展示具体排队位置（避免因位置变化引发客诉）；排队位置及完整队列仅财务端可见 |
| **财务可干预** | 财务可手动调整排队顺序（如有紧急合作方），调整需记录操作日志 |
| **超时告警** | 排队超过 N 小时（可配置，默认24小时）仍未执行，系统升级告警 |

#### 4.3.8 并发提款安全机制

> 当合作方短时间内并发提交多笔提款申请（如双开浏览器标签页）时，需防止因并发读取同一"可提额"导致超额冻结。

**核心策略：乐观锁 + 版本号**

```
合作方提交提款申请
    ↓
读取可提额 + 版本号（version）
    ↓
校验金额（提款额 ≤ 可提额）
    ↓ 通过
执行冻结（UPDATE SET 可提额 -= 提款额, 冻结 += 提款额, version += 1 WHERE version = 原版本号 AND 可提额 >= 提款额）
    ↓ 更新成功（影响行数 = 1）           ↓ 更新失败（影响行数 = 0）
冻结成功，继续审批流               返回"可提额已变动，请刷新后重试"
```

**技术要求**

| 要求 | 说明 |
|---|---|
| 乐观锁字段 | 合作方虚拟账户表增加 `version` 字段（Long 类型，初始值 0） |
| 冻结 SQL | `UPDATE partner_account SET available_amount = available_amount - #{amount}, frozen_amount = frozen_amount + #{amount}, version = version + 1 WHERE partner_id = #{partnerId} AND version = #{expectedVersion} AND available_amount >= #{amount}` |
| 释放 SQL | 驳回/退票释放冻结同样使用乐观锁：`UPDATE ... SET available_amount += #{releaseAmount}, frozen_amount -= #{releaseAmount}, version += 1 WHERE version = #{expectedVersion}` |
| 前端配合 | 提交按钮点击后立即 disable 防止重复点击；收到"版本冲突"响应后自动刷新可提额显示 |
| 重试策略 | 不自动重试，由用户确认最新余额后手动重新操作，避免误操作 |

### 4.4 模块四：合作方入驻与资质审核

#### 4.4.1 入驻流程

```
合作方提交入驻申请（填写基本信息 + 上传资质文件）
    ↓
业务项目组初审（资料完整性）
    ↓ 通过              ↓ 驳回
财务复审（资质合规性）   退回补充材料
    ↓ 通过              ↓ 驳回
入驻成功，开通平台账号   退回补充材料
```

#### 4.4.2 资质审核清单

| 资质类型 | 提交要求 | 审核标准 | 管理规则 |
|---|---|---|---|
| **营业执照** | 加盖公章的彩色扫描件，需在有效期内 | 信息与工商公示一致，无异常经营状态 | 到期前30天提醒更新 |
| **机构法人身份证** | 正反面彩色扫描件 | 与营业执照法人信息一致 | 法人变更时需重新提交 |
| **银行账户信息** | 对公账户的开户许可证/银行回单 | 户名与营业执照公司名称一致 | 变更时需重新审核 |
| **联系人信息** | 姓名、手机号、邮箱、职务 | 信息完整，可联系到人 | 定期更新确认 |

> **注**：具体资质要求需按业内市场要求或法务侧确认，以上为参考标准。

### 4.5 模块五：银行账户管理

#### 4.5.1 合作方银行账户

| 字段 | 类型 | 说明 |
|---|---|---|
| 账户名称 | String | 必须与营业执照公司名称一致 |
| 开户银行 | String | 银行名称 |
| 开户支行 | String | 具体支行名称 |
| 银行账号 | String | 对公账户号 |
| 联行号 | String | 用于跨行转账 |
| 状态 | Enum | 待审核/已生效/已停用 |
| 绑定时间 | DateTime | 首次绑定时间 |

#### 4.5.2 账户绑定/变更流程

- 新增银行账户需提交审核（财务审核）
- 变更账户信息需重新审核
- 提款期间不可变更正在使用的银行账户
- 支持绑定多个银行账户，提款时可选择

### 4.6 模块六：合作方-业务负责人归属管理

> 原始需求明确"业务负责人：可查看所负责合作方的数据"，需维护归属绑定关系。

| 字段 | 类型 | 说明 |
|---|---|---|
| 关系ID | String | 系统自动生成 |
| 合作方 | Reference | 关联的合作方（唯一） |
| 业务负责人 | Reference | 关联的业务项目组成员 |
| 生效时间 | DateTime | 绑定开始生效时间 |
| 失效时间 | DateTime | 可为空，表示长期生效 |
| 状态 | Enum | 生效中 / 已解绑 |

> **MVP 约束**：一个合作方有且仅有一个业务负责人（1:1）；一个业务负责人可管理多个合作方（1:N）。如未来业务需要协同负责人机制，可在 P1 版本扩展。

| 功能 | 说明 |
|---|---|
| **绑定合作方** | 系统管理员或财务为合作方指定唯一的业务负责人 |
| **解绑/变更** | 变更归属时记录完整历史（原负责人、新负责人、变更时间、原因），解绑后原负责人立即失去该合作方数据访问权限 |
| **数据权限联动** | 业务负责人的数据看板、提款审批范围自动按归属关系过滤 |
| **提款审批路由** | 合作方发起提款时，系统根据归属关系自动将审批单路由给对应业务负责人一审 |
| **空缺告警** | 当合作方无绑定业务负责人时，系统应在管理端看板中醒目标识告警，提款申请暂挂直至指定负责人 |

### 4.7 模块七：系统管理

| 功能 | 说明 |
|---|---|
| **用户管理** | 创建/编辑/停用用户账号，分配角色 |
| **角色管理** | 定义角色及对应权限集合 |
| **操作日志** | 记录所有关键操作（规则变更、审批操作、打款操作等），支持审计追溯 |
| **系统配置** | 结算周期默认值、通知模板、银企直联参数（限额、时间窗口、重试策略）、银行结算日历等 |

---

## 五、数据同步与集成

### 5.1 浑天仪数据对接

| 项目 | 说明 |
|---|---|
| **数据源** | 浑天仪（游戏流水数据平台） |
| **同步频率** | T+1，每日凌晨同步前一日数据 |
| **同步内容** | 游戏流水收入数据（按渠道、游戏、日维度） |
| **一致性要求** | 平台展示数据必须与浑天仪保持一致，对账无差异 |
| **异常处理** | 同步失败自动重试（最多3次），失败后告警通知运维 |

### 5.2 银企直联对接

> 采用银企直联方案，通过已部署的银行前置机直连银行核心系统，实现自动化转账。

#### 5.2.1 架构概览

```
分账平台 → 银企直联中间件 → 银行前置机 → 银行核心系统
                                ↑
                         已部署，企业内网
```

#### 5.2.2 核心能力

| 项目 | 说明 |
|---|---|
| **打款方式** | 审批通过后，平台通过银企直联接口向前置机发起转账指令，银行自动执行 |
| **资金池** | 公司自有银行账户（主账户）作为资金池 |
| **资金流转** | 资金池主账户 → 银行内部清算 → 合作方指定银行账户（同行实时/跨行走人行清算） |
| **前置机** | 已部署在企业内网，负责与银行端的通信加密、报文转换、签名验签 |

#### 5.2.3 接口能力

| 接口 | 说明 |
|---|---|
| **单笔转账** | 单笔实时转账指令，适用于单个合作方提款场景 |
| **批量转账** | 批量付款文件提交，适用于结算日集中打款场景（需注意银行批量时间窗口） |
| **交易状态查询** | 根据流水号查询单笔转账的处理状态（处理中/成功/失败/退票） |
| **账户余额查询** | 查询资金池主账户的实时余额，用于资金充足性校验和看板展示 |
| **银行回单下载** | 自动回盘获取电子回单，作为打款凭证归档 |
| **交易流水下载** | 下载银行侧交易明细，用于日终对账 |

#### 5.2.4 银行通道约束

| 约束项 | 说明 | 平台应对策略 |
|---|---|---|
| **单笔限额** | 银行对单笔转账金额有上限（具体以银行合同为准） | 超限时自动拆分为多笔，或提示财务人工处理 |
| **日累计限额** | 单日累计转出金额上限 | 每日打款前校验剩余额度，额度不足时暂停排队并告警 |
| **批量时间窗口** | 银行批量转账通常有受理时间窗口（如工作日 8:30-16:30） | 窗口外的打款请求排队至下一窗口自动执行 |
| **跨行到账时效** | 同行实时到账；跨行走人行大/小额系统，T+0 或 T+1 | 平台记录"打款中"状态，通过状态查询接口轮询结果 |
| **退票风险** | 收款户名/账号不匹配等原因导致银行退票 | 退票回盘后自动更新状态为"已退票"，通知财务人工介入 |
| **节假日限制** | 非工作日大额跨行转账可能延迟 | 预估结算日历，提前提示合作方到账可能延迟 |

#### 5.2.5 对账机制

| 对账类型 | 频率 | 说明 |
|---|---|---|
| **交易对账** | 每日 | 平台打款记录 vs 银行交易流水逐笔核对，差异自动标记待人工处理 |
| **余额对账** | 每日 | 资金池主账户余额 vs 全平台待结算金额汇总，差异超阈值告警 |
| **回单归档** | 每笔 | 打款成功后自动下载电子回单，关联到提款申请单作为凭证 |

#### 5.2.6 降级策略

- 前置机通信异常 → 平台自动将打款状态标记为"待重试"，间隔重试（最多 3 次）
- 重试均失败 → 状态改为"待人工处理"，生成付款指令文件供财务登录网银手动操作
- 降级为单笔级别，不影响其他正常打款批次

---

## 六、非功能需求（EARS 语法）

### 6.1 性能

- **REQ-N-101**：系统应在 2 秒内完成首屏页面加载。
- **REQ-N-102**：当用户执行报表查询（百万级数据量）时，系统应在 3 秒内返回查询结果。
- **REQ-N-103**：系统应支持 200 以上并发用户同时在线操作。
- **REQ-N-104**：当系统执行 T+1 日结算批量计算时，系统应在 30 分钟内完成全部合作方的分账计算。

### 6.2 安全性

- **REQ-N-201**：系统应支持账号密码 + 短信验证码的双因素身份认证。
- **REQ-N-202**：系统应对所有数据传输采用 HTTPS 加密。
- **REQ-N-203**：系统应对银行账号等敏感字段进行脱敏展示和加密存储。
- **REQ-N-204**：系统应基于 RBAC 模型实现权限控制，合作方用户仅可访问本方数据，业务负责人仅可访问所绑定合作方数据。
- **REQ-N-205**：系统应对所有关键操作（规则变更、审批、打款等）进行全量审计记录，日志保留不少于 3 年。
- **REQ-N-206**：当合作方发起提款时，系统应严格校验提款金额 ≤ 可提额，防止超额打款。
- **REQ-N-207**：系统与银行前置机之间的通信应采用加密通道，转账指令需经数字签名验签，确保指令不可篡改、不可抵赖。
- **REQ-N-208**：系统应采用乐观锁机制保护可提额、冻结金额等核心余额字段的并发更新，防止同一合作方并发提款导致超额冻结。

### 6.3 可用性 / 容灾

- **REQ-N-301**：系统年度可用性应达到 99.9%（全年计划外停机不超过 8.76 小时）。
- **REQ-N-302**：系统应执行每日全量数据备份 + 实时增量备份策略。
- **REQ-N-303**：当发生灾难性故障时，系统应在 RPO ≤ 1 小时、RTO ≤ 4 小时内完成恢复。
- **REQ-N-304**：当浑天仪数据同步失败时，系统应自动重试（最多 3 次），3 次均失败后向运维发送告警通知。

---

## 七、用户流程

> Skill 要求描述 2-3 个关键场景的完整操作步骤。

### 7.1 合作方提款流程（端到端）

1. 合作方登录平台，在"首页"看到待结算金额、可提额、冻结金额三张卡片。
2. 合作方进入"提款管理 → 发起提款"页面。
3. 系统展示当前可提额、单笔/每日提款上限及今日已用额度、已绑定的银行账户列表。
4. 合作方填写提款金额、选择收款银行账户、填写备注（可选）。
5. 合作方点击"提交申请"，系统校验：金额 ≤ 可提额、≤ 单笔上限、当日累计未超限。
6. 校验通过 → 系统将对应金额从可提额转入冻结金额、创建审批单，推送通知给合作方对应的业务负责人。
7. 业务负责人在"待处理提款申请"中查看审批单，审核通过/驳回。
8. 一审通过 → 系统推送通知给财务终审人。
9. 财务终审人审核通过 → 系统通过银企直联接口向前置机发起转账指令。
10. 银行处理完成 → 系统通过状态查询接口获取结果：
    - 成功 → 扣减待结算金额（冻结→已结算），自动下载电子回单归档，标记"已完成"，通知合作方。
    - 失败/退票 → 标记对应状态，释放冻结金额（冻结→可提额），通知财务人员介入。
11. 前置机通信异常 → 自动重试（最多3次），均失败后生成付款指令供财务手动网银操作。

### 7.2 财务创建分成规则流程

1. 财务人员进入"分成规则管理 → 创建规则"页面，可选择"空白创建"或"从已有规则复制"。
2. 若选择复制，系统加载源规则的参数（分成结构、结算周期等），合作方和游戏产品需重新选择。
3. 财务填写/修改参数：适用合作方、适用游戏产品、分成比例、生效/失效时间。
4. 财务点击"提交审批"，系统创建规则变更审批单。
5. 系统推送通知给对应业务负责人。
6. 业务负责人审核通过 → 推送给财务负责人。
7. 财务负责人审核通过 → 规则状态变为"生效中"，在下一次 T+1 分账计算时自动应用。
8. 任一环节驳回 → 退回给发起人修改，附驳回原因。

### 7.3 合作方入驻流程

1. 合作方通过平台入口提交入驻申请，填写公司基本信息。
2. 系统引导上传资质文件：营业执照、法人身份证、银行账户信息、联系人信息。
3. 合作方确认提交，系统创建入驻审核单。
4. 业务项目组收到通知，进行初审（资料完整性检查）。
5. 初审通过 → 财务人员收到通知，进行复审（资质合规性检查）。
6. 复审通过 → 系统自动开通合作方平台账号，分配"合作方"角色权限，通知合作方。
7. 任一环节驳回 → 退回合作方补充材料，附具体原因。
8. 入驻完成后，系统管理员或财务为该合作方指定业务负责人（绑定归属关系）。

---

## 八、前端技术方案

**项目类型**：企业管理后台

**框架选型**：
- 框架：UmiJS + Ant Design Pro（React 技术栈）
- 原因：内置权限管理、菜单路由、多环境配置，表格表单开箱即用，适合复杂B端管理系统

**UI 组件库**：Ant Design / Ant Design Pro Components

**本需求涉及的组件**：

| 功能模块 | 使用组件 | 来自哪个库 |
|---|---|---|
| 数据看板-收入趋势图 | Line/Column Chart | @ant-design/charts |
| 数据看板-汇总卡片 | StatisticCard | @ant-design/pro-card |
| 合作方/规则列表 | ProTable | @ant-design/pro-table |
| 规则创建/编辑表单 | ProForm / StepsForm | @ant-design/pro-form |
| 提款申请表单 | ProForm + Modal | @ant-design/pro-form |
| 审批详情展示 | Descriptions | antd |
| 审批流程可视化 | Steps / Timeline | antd |
| 资质文件上传 | Upload + Dragger | antd |
| 银行账户管理 | ProTable + ModalForm | @ant-design/pro-table |
| 消息通知 | Notification + Badge | antd |
| 权限管理 | ProTable + Tree | @ant-design/pro-table |
| 操作日志 | ProTable | @ant-design/pro-table |

**组件缺口（如有）**：
- 审批流引擎可视化：antd Steps 可满足简单审批流展示，如需复杂的审批流设计器，建议引入 `@logicflow/core`（GitHub ⭐ 7k+，专业流程图引擎）

---

## 九、页面结构与导航

### 9.1 合作方端

```
├── 首页（数据概览）
│   ├── 待结算金额 / 可提额 / 冻结金额 卡片组
│   ├── 累计收入卡片
│   └── 收入趋势图
├── 收入明细
│   ├── 收入列表（按日/游戏筛选）
│   └── 分成明细（关联规则展示）
├── 提款管理
│   ├── 发起提款
│   └── 提款记录
├── 账户管理
│   ├── 银行账户列表
│   └── 账户信息变更
└── 消息中心
```

### 9.2 管理端（业务 + 财务）

```
├── 数据看板
│   ├── 收入大盘
│   ├── 合作方分成汇总
│   └── 资金账户总览
├── 提款管理
│   ├── 待处理申请
│   ├── 全部提款记录
│   └── 打款记录
├── 分成规则管理
│   ├── 规则列表（含创建/复制入口）
│   └── 规则审批
├── 合作方管理
│   ├── 合作方列表
│   ├── 入驻审核
│   ├── 银行账户审核
│   └── 归属关系管理
├── 系统管理
│   ├── 用户管理
│   ├── 角色权限
│   ├── 操作日志
│   └── 系统配置
└── 消息中心
```

---

## 十、核心业务规则汇总

| 编号 | 规则 | 说明 |
|---|---|---|
| BR-01 | 分账时效 | T+1自动计算，数据同步自浑天仪 |
| BR-02 | 提款限额管控 | 提款金额需同时满足：≤ 可提额、≤ 单笔上限、当日累计 ≤ 每日上限；限额参数由财务按合作方配置 |
| BR-03 | 规则修改权限 | 仅财务角色可创建/修改/停用分成规则 |
| BR-04 | 规则修改审批 | 规则变更需业务负责人 + 财务负责人双重审批 |
| BR-05 | 规则版本机制 | 每次修改参数自动生成新版本，旧版本冻结不可篡改；分账计算按流水日期匹配当时生效的版本；分成明细关联具体版本ID |
| BR-06 | 提款审批 | 业务一审 + 财务终审，双重复核 |
| BR-07 | 打款执行 | 终审通过后通过银企直联自动发起转账，前置机异常时降级为人工网银操作 |
| BR-08 | 金额流转与冻结 | 提款申请提交即冻结（可提额→冻结金额）；打款成功扣减待结算金额（冻结金额→已结算金额）；驳回/退票释放（冻结金额→可提额）；**打款失败冻结不自动释放**，由财务人工决定重新打款或释放冻结；提款校验基于可提额 |
| BR-19 | 资金回账前置确认 | 打款失败或退票后，**财务必须通过银行流水/余额查询确认打款金额已退回公司资金池，才可手动解冻合作方冻结金额**；未确认回账前系统禁止解冻，防止出现"打款金额仍滞留在银行侧、合作方已可重新申请"的双重占用风险 |
| BR-09 | 银行账户审核 | 新增/变更银行账户需财务审核通过后方可使用 |
| BR-10 | 数据隔离 | 合作方仅能查看与自身关联的数据 |
| BR-11 | 游戏维度核算 | 同一合作方的多款游戏独立核算收入与分成，分成规则按"合作方 + 游戏产品"组合配置 |
| BR-12 | 业务负责人数据范围 | 业务负责人仅能查看和操作所绑定合作方的数据，提款审批自动路由至对应负责人 |
| BR-13 | 财务最高权限 | 财务角色拥有最高业务权限，可查看全平台数据、管理所有规则、执行终审 |
| BR-14 | 银企直联降级 | 前置机通信异常自动重试 3 次，均失败后降级为生成付款指令，由财务登录网银手动操作；降级为单笔级别 |
| BR-16 | 退票处理 | 银行退票后自动释放冻结金额（回到可提额），状态标记为"已退票"，通知财务和合作方 |
| BR-17 | 资金池不足排队 | 资金池余额不足时按"先审先付"顺序部分打款，余额耗尽后剩余进入排队，资金补充后自动恢复；排队超时升级告警 |
| BR-18 | 回单自动归档 | 打款成功后自动下载银行电子回单，关联提款申请单作为凭证 |
| BR-15 | 资质到期提醒 | 合作方营业执照等资质文件到期前 30 天自动提醒更新 |

---

## 十一、里程碑与迭代计划

### P0 — MVP 核心版本（预估 8-10 周）

| 阶段 | 周期 | 交付内容 |
|---|---|---|
| 第一阶段：基础框架 | 第 1-2 周 | 项目脚手架搭建、数据库设计、用户权限体系、浑天仪数据对接 |
| 第二阶段：分账核心 | 第 3-5 周 | 分成规则管理（模板 + CRUD + 审批）、自动分账计算引擎、数据看板（合作方视图 + 公司视图） |
| 第三阶段：提款闭环 | 第 6-8 周 | 合作方入驻与资质审核、银行账户管理、提款申请与双重审批、银企直联对接与自动打款、回单归档与对账 |
| 第四阶段：上线保障 | 第 9-10 周 | 集成测试、UAT 验收、安全审计、灰度上线 |

### P1 — 扩展功能（Backlog）

> 以下功能不在 v1.0 范围内，供后续版本规划参考。

| 优先级 | 功能 | 理由 |
|---|---|---|
| 高 | 阶梯式分成规则 | 底层已预留扩展能力，业务侧有明确诉求（根据收入规模浮动比例） |
| 高 | 数据报表导出与定制化 | 财务和合作方均有导出需求，降低线下对账工作量 |
| 中 | 合作方自助注册 + 电子签约 | 当前依赖人工开通账号，合作方数量增长后效率瓶颈明显 |
| 中 | 移动端适配（合作方 H5 端） | 合作方查看收入和提款状态的高频场景适合移动端 |
| 低 | 多币种结算支持 | 若出海业务发展则需要，当前暂无明确需求 |
| 中 | 规则版本 diff 对比 | MVP 仅支持查看单个版本快照，后续可增加任意两版参数高亮对比功能 |
| 中 | 规则模板系统 | MVP 已支持"从已有规则复制"，规则量增长后可引入正式模板管理（模板固化通用字段、版本管理） |
| 低 | 审批流自定义设计器 | 当前两级固定审批链已满足，复杂审批链需求不明确 |

---

## 十二、设计建议

> 以下是文档中值得在产品讨论中重点审查的设计决策，供评审参考。

- **待结算金额与资金池的对账一致性**：全平台待结算金额汇总应始终 ≤ 资金池实际余额，但分账计算误差或打款异常可能导致两者不一致。 → 建议：建立每日自动对账任务，差异超过阈值时即时告警，财务人工核实。

- **业务负责人归属关系的交接风险**：业务负责人离职/调岗时，若归属关系未及时变更，会导致提款审批单无人处理，或已离岗人员仍有数据权限。 → 建议：归属关系变更即时生效，"待一审"的提款单自动重新路由至新负责人；无负责人的合作方在看板中醒目标识告警。

- **银企直联的批量打款时间窗口管理**：银行批量转账通常有受理时间窗口（如工作日 8:30-16:30），窗口外提交或节假日的提款审批通过后无法立即执行。 → 建议：平台维护一套"银行结算日历"，窗口外通过的打款自动排队至下一可用窗口；合作方侧展示预计到账时间而非固定承诺，避免预期差。

---

## 十三、待确认假设

以下内容因原始描述不够明确，基于合理假设撰写，**请确认或修正**：

1. 假设**一个合作方可关联多款游戏产品**，每款游戏有独立分成规则，如有不同请告知。
2. 假设**业务负责人与合作方为 1:N 关系**（一个负责人可管理多个合作方，一个合作方有且仅有一个业务负责人），如需支持协同负责人请告知。
3. 假设**提款不设最低金额限制**（但有单笔上限和每日累计上限），如有最低门槛或手续费规则请补充。
4. 假设**合作方银行账户仅限对公账户**（与营业执照名称一致），不支持个人账户收款，如有不同请告知。
5. 假设**分成规则审批流为两级固定链**（业务负责人 → 财务负责人），不支持自定义审批链或加签，如有不同请告知。
6. 假设**当前银企直联仅对接一家银行**（主账户开户行），跨行转账走该银行的人行清算通道，如有多家银行前置机请告知。
7. 假设**同行转账实时到账，跨行转账 T+0 或 T+1**（取决于金额大小及人行清算批次），如有更高时效要求请告知。
8. 假设**虚拟账户不支持合作方之间余额划转**，每个账户独立隔离，如有不同请告知。

---

## 十四、开放问题

| 编号 | 问题 | 当前状态 | 需要谁确认 |
|---|---|---|---|
| Q-01 | 费用扣除项的具体类型和计算方式 | 待补充 | 财务 + 业务 |
| Q-02 | 合作方资质审核的具体行业标准 | 需法务确认 | 法务 |
| Q-03 | 银企直联银行侧的单笔/日累计限额具体数值 | 需确认银行合同约定 | 财务 + 技术 |
| Q-04 | 消息通知渠道：仅站内信还是需要短信/邮件 | 待确认 | 产品 + 业务 |
| Q-05 | 结算周期的具体可选范围（日结/周结/月结） | 待确认 | 财务 |
| Q-06 | 是否需要与现有身边云平台打通 | 待确认（PDF 提到参考身边云） | 技术 + 业务 |
| Q-07 | 规则审批流是否对接现有 OA 系统 | 待确认 | 技术 + 业务 |
| Q-08 | 业务负责人-合作方归属关系的初始化方式 | 批量导入 or 手动绑定，待确认 | 业务 + 系统管理 |
| Q-09 | 银行批量转账的受理时间窗口及节假日规则 | 需确认银行合同/技术文档 | 技术 + 财务 |
| Q-11 | 银企直联前置机是否支持多银行扩展 | 待确认 | 技术 |
| Q-12 | 银行退票的回盘时效（多久能收到退票通知） | 需确认银行技术文档 | 技术 |
| Q-10 | 合作方提款是否有最低金额限制或手续费 | 待确认 | 财务 |
| Q-13 | 单笔/每日提款上限的默认值及各合作方的差异化配置 | 待财务确定 | 财务 |
| Q-14 | 平台每日打款总额上限的默认值 | 待财务确定 | 财务 |
| Q-15 | 资金池排队超时的告警阈值（默认24小时是否合适） | 待确认 | 财务 + 运维 |
| Q-16 | 分成计算为负值时的业务处理策略（记0 / 挂账抵扣 / 线下追缴） | 待确认 | 财务 + 业务 |

---

## 十五、重点测试用例

### 打款失败资金回账场景（⭐ 重点）

> **背景**：曾发生过打款失败后，金额滞留在银行侧（未回到公司资金池）的情况，导致若提前释放冻结将造成资金双重占用风险。以下测试用例为必测场景。

#### TC-PAY-001：打款失败 — 资金已回账 — 正常解冻

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 财务终审通过，系统发起打款 | 提款单状态变为"打款中" |
| 2 | 银行返回打款失败（RJCT） | 提款单状态变为"待人工处理"，冻结金额**不自动释放** |
| 3a | 系统通过流水查询接口（300001）检测到匹配回账入账记录 | 系统自动标记"疑似已回账"，推送通知财务复核 |
| 3b | 财务点击确认复核通过 | 系统解冻冻结金额，释放回合作方可提额；提款单状态变为"已关闭-资金已回账" |
| 4 | 合作方重新发起提款申请 | 申请成功，可提额正确扣减 |

**兜底路径（自动检测失败时）**

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3c | 自动检测超时未匹配，财务手动登录网银核查 | 财务在系统勾选"已确认资金回账"并上传流水截图 |
| 3d | 财务提交手动确认 | 系统解冻，操作记录凭证截图，审计日志留存 |

#### TC-PAY-002：打款失败 — 资金**未**回账 — 禁止解冻

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 银行返回打款失败 | 提款单状态变为"待人工处理"，冻结金额不释放 |
| 2 | 财务查询银行流水，确认资金**尚未退回**（仍在银行处理中） | 财务备注"待资金回账"，不操作解冻 |
| 3 | 合作方尝试重新发起同等金额提款申请 | 系统拒绝：可提额不足（冻结金额仍占用），提示"存在未完结打款，请等待财务确认资金回账后再试" |
| 4 | 银行资金退回后，财务确认回账并解冻 | 冻结金额释放，合作方恢复可提额，可正常申请 |

#### TC-PAY-003：退票 — 资金回账确认

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 银行返回退票（RTRN） | 提款单状态变为"已退票"，冻结金额**不自动释放** |
| 2a | 系统通过流水查询接口（300001）检测到退票回账入账记录 | 系统自动标记"疑似已回账"，推送通知财务复核确认 |
| 2b | 财务确认复核通过 | 系统解冻，释放回合作方可提额，合作方可修改账户后重新申请 |
| 3 | 若退票金额**未回账**，财务选择"等待回账" | 系统保持冻结，提款单标记"退票-待资金回账"；不允许合作方重新申请 |
| 4 | 退票金额回账后，系统自动检测或财务手动确认 | 解冻完成，流程同 TC-PAY-001 兜底路径 |

#### TC-PAY-004：资金池余额与冻结金额一致性校验

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 构造场景：2笔打款失败，金额分别滞留银行 | 全平台冻结金额 > 公司资金池实际可用余额 |
| 2 | 执行日终对账任务 | 系统检测到"冻结金额汇总 > 资金池余额"，触发告警通知财务 |
| 3 | 财务逐笔确认回账状态后解冻 | 资金池余额与冻结金额恢复一致，告警自动消除 |

### 附录A：EARS 语法速查

| 句型 | 模板 | 适用场景 |
|---|---|---|
| 普遍性 | 系统应 [动作]。 | 始终成立的系统行为 |
| 事件驱动 | 当 [触发事件] 时，系统应 [响应]。 | 用户操作或系统事件触发的行为 |
| 状态驱动 | 当系统处于 [状态] 时，系统应 [行为]。 | 特定状态下的持续行为 |

### 附录B：术语表

| 术语 | 说明 |
|---|---|
| 大发行模式 | 公司代理发行第三方游戏产品的合作模式 |
| CP | Content Provider，游戏内容提供方/开发商 |
| 虚拟账户 | 合作方在平台上的电子余额账户，非实体银行账户 |
| 分成金额 | 单次 T+1 分账计算产生的合作方应得金额 |
| 累计分成金额 | 合作方历史所有分成金额的总和 |
| 待结算金额 | 合作方虚拟账户当前总余额 = 累计分成金额 − 已结算金额 |
| 可提额 | 待结算金额中可发起提款的部分 = 待结算金额 − 冻结金额 |
| 冻结金额 | 已发起提款申请但未到账的部分（审批中/排队中/打款中），不可重复提款 |
| 已结算金额 | 合作方历史成功提款（已到账）的累计总额 |
| 资金池 | 公司自有银行账户，用于存放收入及支付合作方分成 |
| 浑天仪 | 公司内部游戏数据平台，提供流水收入数据 |
| T+1 | 数据延迟一个自然日更新 |
| 分成规则 | 定义收入在公司与合作方之间分配比例的配置 |
| 归属关系 | 合作方与业务负责人之间的管理绑定关系，决定数据可见范围和审批路由 |
| 银企直联 | 企业通过前置机直连银行核心系统的电子银行通道，实现自动化转账、查询、对账 |
| 前置机 | 部署在企业内网的硬件设备，负责与银行端的通信加密、报文转换和签名验签 |
| 退票 | 银行因收款户名/账号不匹配等原因将已发起的转账款项退回付款账户 |
| 回单/回盘 | 银行完成转账后返回的电子凭证，证明付款事实 |
| EARS | Easy Approach to Requirements Syntax，简易需求语法 |

### 附录C：参考系统

- 支付宝提现流程（提款 UX 参考）
- 身边云平台（现有结算平台参考）
- 3.18 会议纪要《财务分账平台自动提现方案》
- 现有银企直联前置机技术文档（银行侧）
