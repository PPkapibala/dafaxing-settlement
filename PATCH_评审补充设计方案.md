# 大发行打款分账系统 — 评审待讨论事项

| 项目 | 内容 |
|---|---|
| **关联 PRD** | PRD_大发行打款分账系统.md V1.2 |
| **创建日期** | 2026-03-25 |
| **文档性质** | 评审中发现的待讨论设计方案，需团队评审确认后决定是否纳入 PRD |

---

## 议题一：分账计算前数据质量校验（原评审 #6）

### 问题描述

当前 PRD 假设浑天仪 T+1 数据同步完成后直接触发分账计算，未定义数据质量校验环节。若源数据存在异常（如某游戏当日流水为 0 但前日为百万级、重复数据、金额为负等），系统会"忠实地计算出错误结果"，错误入账后需人工追溯修正，成本极高。

### 设计方案

#### 1. 校验时机

```
浑天仪 T+1 数据同步完成
    ↓
数据质量校验（自动）
    ↓ 全部通过                    ↓ 存在异常
自动触发分账计算              暂停该批次计算
                              生成"数据异常报告"
                              告警通知财务 + 运维
                              ↓
                         财务审核并处理
                          ↓ 确认数据无误          ↓ 数据需修正
                       手动触发计算           联系数据团队修正后重新同步
```

#### 2. 校验规则清单

| 编号 | 校验规则 | 严重级别 | 处理方式 |
|---|---|---|---|
| DQ-01 | 同步数据总条数为 0（无数据到达） | 严重 | 阻断计算 + 告警 |
| DQ-02 | 某游戏当日收入较前 7 日均值波动超过 ±80% | 警告 | 标记异常 + 通知财务确认，不自动阻断 |
| DQ-03 | 存在金额为负的流水记录 | 严重 | 剔除该记录 + 告警，其余正常计算 |
| DQ-04 | 发现重复流水（相同渠道+游戏+时间+金额） | 严重 | 阻断该游戏计算 + 告警 |
| DQ-05 | 数据同步时间晚于预期窗口超过 N 小时 | 警告 | 仅告警，不阻断 |

#### 3. 数据异常报告字段

| 字段 | 说明 |
|---|---|
| 报告ID | 系统自动生成 |
| 批次日期 | T+1 对应的流水日期 |
| 异常类型 | 对应校验规则编号 |
| 涉及游戏 | 异常数据关联的游戏产品 |
| 异常详情 | 具体数值/条目信息 |
| 处理状态 | 待处理 / 已确认通过 / 已修正重算 |
| 处理人 | 财务操作人 |
| 处理时间 | 操作时间 |

#### 4. 若纳入 PRD 的影响

- 新增 REQ-F-007：`当 T+1 数据同步完成时，系统应先执行数据质量校验，校验通过后方可触发分账计算；存在严重级别异常时阻断计算并告警。`
- REQ-F-001 前置条件需增加"数据质量校验通过"

### 待讨论要点

- [ ] 校验规则 DQ-02 波动阈值 ±80% 是否合理？是否需按游戏体量分级设定？
- [ ] 数据异常阻断后的手动触发权限：仅财务还是运维也可操作？
- [ ] 是否需要纳入 P0 还是作为 P1 增强？

---

## 议题二：浑天仪数据修正与冲正机制（原评审 #12）

### 问题描述

当前 PRD 假设浑天仪 T+1 数据一旦同步即为最终数据，未考虑数据修正场景。实际业务中，渠道回调延迟、退款冲销、数据平台 Bug 等原因可能导致已同步的历史数据被修正。若已基于错误数据完成了分账计算甚至打款，缺乏冲正机制将导致账目混乱。

### 设计方案

#### 1. 修正触发方式

| 触发方式 | 说明 |
|---|---|
| 浑天仪主动推送 | 浑天仪数据平台检测到修正后，通过接口推送"数据修正通知"，包含修正的日期范围和游戏 |
| 财务手动触发 | 财务在后台发起"数据重算"请求，指定合作方、游戏、日期范围 |

#### 2. 冲正计算流程

```
收到数据修正通知（指定日期 D、游戏 G）
    ↓
查找日期 D + 游戏 G 的原始分成计算记录
    ↓
标记原记录为"已冲正"（保留不删除）
    ↓
基于修正后的收入数据 + 原规则版本重新计算
    ↓
生成新分成记录（类型标记为"冲正"，关联原记录ID）
    ↓
计算差额 = 新分成金额 - 原分成金额
    ↓ 差额 > 0（应补）               ↓ 差额 < 0（应扣）
差额追加到待结算金额              差额从待结算金额扣减
                                    ↓ 扣减后可提额 < 0
                                  可提额置为0，差额挂账
                                  通知财务线下追缴
```

#### 3. 冲正记录数据结构

| 字段 | 类型 | 说明 |
|---|---|---|
| 冲正ID | String | 系统自动生成 |
| 原分成记录ID | Reference | 被冲正的原始计算记录 |
| 原分成金额 | Decimal | 修正前的计算结果 |
| 新分成金额 | Decimal | 修正后的计算结果 |
| 差额 | Decimal | 新 - 原，正数为补发，负数为追扣 |
| 冲正原因 | String | 修正原因说明 |
| 触发方式 | Enum | 系统自动 / 财务手动 |
| 处理状态 | Enum | 已自动处理 / 待人工处理（差额为负且余额不足时） |
| 操作人 | Reference | 手动触发时记录操作人 |
| 操作时间 | DateTime | 冲正执行时间 |

#### 4. 关键约束

| 约束 | 说明 |
|---|---|
| 已打款的不可自动扣回 | 若差额为负且对应金额已打款到合作方银行账户，系统仅做挂账处理，由财务线下与合作方协商退回 |
| 冲正需审批 | 财务手动触发的冲正需走审批流（财务发起 → 财务负责人审批），防止误操作 |
| 审计追溯 | 原记录不删除，通过"已冲正"标记 + 新记录的"冲正"类型，完整保留修改前后数据链 |

#### 5. 若纳入 PRD 的影响

- 建议新增模块"数据冲正管理"或在"分账计算引擎"章节增加冲正子流程
- 新增 REQ-F-008：`当浑天仪推送数据修正通知时，系统应自动定位受影响的分成记录，执行冲正计算，差额自动调整待结算金额；差额为负且余额不足时挂账并通知财务人工处理。`
- 合作方看板需增加"冲正记录"查看入口

### 待讨论要点

- [ ] 浑天仪数据平台是否支持主动推送修正通知？需与数据团队确认接口能力
- [ ] 冲正场景在实际业务中的发生频率？是否值得 P0 投入？
- [ ] 已打款金额的线下追缴流程是否需要系统化支持（生成追缴单据/记录协商结果）？
- [ ] 冲正对合作方的可见性：合作方是否能看到冲正记录和差额明细？

---

## 议题三：资金看板精简——已确认待付替代预估图表（原评审 #15）

### 问题描述

当前 PRD 4.1.2 公司财务视图中设计了"预估资金需求图表"，该功能依赖对未来提款行为的预测，实际准确性极低（取决于合作方何时提款、提多少），可能误导财务决策。

### 替代方案

用"已确认资金需求"替代"预估资金需求"，仅展示已经产生的、确定性的资金压力数据。

#### 1. 资金看板改进设计

**替换前**（PRD 原设计）：

| 子模块 | 说明 |
|---|---|
| 资金账户总览 | 余额卡片 + 出入金流水时间线 + **预估资金需求图表** |

**替换后**：

| 子模块 | 功能描述 | 交互说明 |
|---|---|---|
| **资金池余额** | 实时银行余额（银企直联查询） | 大数字卡片，每日自动刷新，支持手动刷新 |
| **今日待打款** | 当日已通过终审待执行的打款总额 | 数字卡片，与余额卡片并列展示，余额不足时红色告警 |
| **排队中总额** | 因资金不足排队等待的打款总额及笔数 | 数字卡片，点击可展开排队明细列表 |
| **全平台待结算总额** | 所有合作方待结算金额汇总 | 数字卡片，代表平台理论上的最大资金义务 |
| **出入金流水** | 近 30 日资金池出入金流水时间线 | 列表形式，标注每笔用途（打款/入账/手动调整） |

#### 2. 关键指标公式

```
资金缺口 = 今日待打款 + 排队中总额 − 资金池余额
（缺口 > 0 时红色展示，提示财务需补充资金）
```

#### 3. 若纳入 PRD 的影响

- 4.1.2 "资金账户总览" 行的描述从 `余额卡片 + 出入金流水时间线 + 预估资金需求图表` 改为上述五项确定性指标
- 去掉"预估资金需求图表"相关的前端图表组件需求
- 前端组件使用 `StatisticCard`（@ant-design/pro-card）即可实现，无需引入图表库

### 待讨论要点

- [ ] 财务团队是否确实不需要预估能力？是否有其他渠道（如 Excel）已在做资金预估？
- [ ] "全平台待结算总额"是否会引起不必要的资金压力焦虑（因合作方不一定会全部提款）？
- [ ] 是否需要增加"近 7 日打款趋势"辅助财务判断资金调度节奏？

---

## 议题与 PRD 条目对照表

| 议题编号 | 原评审编号 | 问题分类 | PRD 受影响章节 | 建议纳入阶段 | 当前状态 |
|---|---|---|---|---|---|
| 议题一 | #6 | 遗漏边界 | 3.1 分账计算引擎、5.1 数据同步 | P0 详设阶段 | 待讨论 |
| ~~PATCH-02~~ | ~~#8~~ | ~~遗漏边界~~ | ~~3.3 提款与打款~~ | ~~P0~~ | **已纳入 PRD V1.2**（REQ-F-216 + REQ-N-208 + 4.3.8） |
| 议题二 | #12 | 遗漏边界 | 3.1 分账计算引擎、4.1 数据看板 | P0/P1 | 待讨论 |
| 议题三 | #15 | 过度设计 | 4.1.2 公司财务视图 | P0（替换原设计） | 待讨论 |
