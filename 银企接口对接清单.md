# 大发行打款分账系统 — 银企接口对接清单

**文档版本**: V1.0  
**创建日期**: 2026-04-01  
**关联需求文档**: PRD_大发行打款分账系统.md V1.2  
**关联规范**: 中国银行银企对接接口规范 V2

---

## 一、接口总体情况

| 指标 | 数值 |
|-----|-----|
| **规范中标准接口数** | 12个 |
| **P0 必需接口(MVP)** | **6个** ✅ |
| **P1 重要接口(阶段2)** | 6个 |
| **P2 建议接口(阶段3)** | 3个 |
| **PRD明确需求映射** | 6/6 完全对应 ✅ |
| **新增建议接口** | 1个(退票处理) |

### 需求-规范映射矩阵

| PRD需求接口 | 功能需求编码 | 规范中对应接口 | 接口编码 | 对齐度 | 详细说明 |
|-----------|----------|----------|--------|------|--------|
| ✅ 单笔转账 | REQ-F-203 | 单笔转账 | 100001 | 完全对应 | 发起转账、转账查询 |
| ✅ 交易状态查询 | REQ-F-203~210 | 单笔转账查询 | 100002 | 完全对应 | 查询打款结果、轮询状态 |
| ✅ 账户余额查询 | REQ-F-209 | 账户余额查询 | 200001 | 完全对应 | 查询资金池主账户余额 |
| ✅ 批量转账 | REQ-F-203 | 批量转账 + 批量查询 | 100003/100004 | 完全对应 | 结算日集中打款 |
| ✅ 银行回单下载 | REQ-F-208 | 回单下载 | 400001 | 完全对应 | 下载电子回单凭证 |
| ✅ 交易流水下载 | REQ-F-205/206 | 流水查询 | 300001 | 完全对应 | 日终对账、交易明细查询 |
| ❓ 退票处理 | REQ-F-206 | 退票查询/通知 | 500001/500002 | 需评估 | 原PRD未显式提及，但打款失败/退票处理需要 |

---

## 二、P0 必需接口清单(MVP阶段)

### 接口汇总表

| # | 接口编码 | 接口名称 | 调用时机 | 限额/约束 | 优先级 |
|---|--------|--------|--------|---------|------|
| 1️⃣ | **100001** | **单笔转账** | 提款批准后→发起转账 | 单笔≤500w/日≤5000w | **P0** |
| 2️⃣ | **100002** | **转账状态查询** | 打款后→轮询查询(30s x10次) | 需处理4种结果(ACPT/SUCC/RJCT/RTRN) | **P0** |
| 3️⃣ | **200001** | **账户余额查询** | 打款前+看板实时 | 限频1次/分钟,缓存30秒 | **P0** |
| 4️⃣ | **100003** | **批量转账** | 结算日集中打款 | XML文件格式,可用单笔替代 | **P0** |
| 5️⃣ | **400001** | **回单下载** | 打款成功后→获取凭证 | 定时轮询5分钟x5次 | **P0** |
| 6️⃣ | **300001** | **流水查询** | 日终对账+合作方查询 | 支持分页、按日期查询 | **P0** |

### 接口1: 单笔转账 (100001)

**功能描述**: 发起单笔实时转账交易，用于合作方提款支付

**请求参数**:
```xml
<SinglePay>
  <DebtAccountNbr>1234567890</DebtAccountNbr>        <!-- 付款账户(资金池主账户) -->
  <CreditAccountNbr>9876543210</CreditAccountNbr>    <!-- 收款账户 -->
  <CreditAccountName>XX科技有限公司</CreditAccountName> <!-- 收款人名称(必须与账户名一致!) -->
  <CreditBankCode>ABC0000001</CreditBankCode>       <!-- 收款行编码(标识同行/跨行) -->
  <DebitAmount>100000.00</DebitAmount>              <!-- 转账金额(到两位小数) -->
  <Currency>CNY</Currency>                          <!-- 币种 -->
  <PostingText>3月份分成款</PostingText>            <!-- 摘要/附言(极其重要!) -->
  <Reference>WD-20260324001</Reference>             <!-- 参考号(必须关联业务单号) -->
  <OrderingPartyName>公司名称</OrderingPartyName>   <!-- 付款人名称 -->
</SinglePay>
```

**返回参数**:
```xml
<Response>
  <BankSerial>202603241234567890</BankSerial>    <!-- 银行流水号(唯一标识) -->
  <TransactionStatus>ACPT</TransactionStatus>    <!-- 处理状态 -->
  <ErrorCode>000000</ErrorCode>                  <!-- 错误码 -->
  <ErrorMessage>成功</ErrorMessage>              <!-- 错误信息 -->
  <ExecuteTime>2026-03-24 14:30:00</ExecuteTime> <!-- 执行时间 -->
</Response>
```

**返回状态码**:
| 状态码 | 含义 | 系统处理 |
|-------|------|--------|
| ACPT | 已接受(正在处理) | → "打款中"状态，启动轮询查询 |
| SUCC | 成功 | → 标记成功，扣减待结算，下载回单，通知合作方 |
| RJCT | 拒绝 | → 金额超限/账号不存在/账户冻结等，标记失败，释放冻结金额 |
| RTRN | 已返回(退票) | → 账户/金额不匹配等原因退票，释放冻结金额，通知财务 |
| PEND | 待处理 | → 仍在队列中，继续轮询 |
| CANC | 已取消 | → 人工取消或银行拒绝 |

**限额与时间窗口**:
```
单笔限额: 500万元/笔
日累计限额: 5000万元/日  (需通过200002接口校验)
时间窗口: 工作日 08:30-16:30
  - 时间内提交: 立即处理
  - 时间外提交: 排队至下一工作日08:30
同行: 实时到账(通常1小时内)
跨行: T+0/T+1(根据金额和人行清算批次)
```

**核心约束** ⚠️:
- 收款人名称**必须**与收款账户名一致，否则银行会直接退票
- 参考号(Reference)必须**唯一**，建议用"WD-日期-序列号"格式
- 超过单笔限额需自动拆分为多笔
- 需实现**重试机制**: 最多3次，间隔30秒

---

### 接口2: 单笔转账查询 (100002)

**功能描述**: 查询单笔转账交易的处理状态，用于轮询确认打款结果

**轮询查询流程**:
```
发起转账(100001)
  ↓ 返回ACPT
启动轮询 (最多10次,间隔30s)
  ├─ 第1次查询(1分钟后)
  ├─ 第2-10次查询(每次间隔30秒)
  └─ 若10次后仍PEND → 标记"待人工处理"
```

**请求参数**:
```xml
<QuerySinglePay>
  <BankSerial>202603241234567890</BankSerial>  <!-- 从100001返回的流水号 -->
  <!-- 或 -->
  <Reference>WD-20260324001</Reference>         <!-- 业务参考号 -->
</QuerySinglePay>
```

**返回状态处理**:
| 场景 | 返回状态 | 系统处理 |
|------|---------|--------|
| 打款成功 | SUCC | ✅ 更新提款单状态为"已完成"，解冻金额，下载回单 |
| 仍在处理 | ACPT/PEND | ⏳ 继续轮询(间隔30秒) |
| 账户名不匹配 | RTRN | ❌ 更新为"已退票"，释放冻结金额，通知财务+合作方 |
| 金额超限 | RJCT | ❌ 标记失败，释放冻结金额，提示财务拆分转账 |
| 账户不存在 | RJCT | ❌ 标记失败，通知合作方修改银行账户信息 |

---

### 接口3: 账户余额查询 (200001)

**功能描述**: 查询资金池主账户的实时余额，用于资金充足性校验和看板展示

**请求参数**:
```xml
<BalanceQuery>
  <AccountNbr>1234567890</AccountNbr>  <!-- 资金池主账户号 -->
  <CurrencyCode>CNY</CurrencyCode>     <!-- 币种 -->
</BalanceQuery>
```

**返回参数**:
```xml
<BalanceResponse>
  <Balance>1000000.00</Balance>          <!-- 可用余额 -->
  <BalanceType>BOOK</BalanceType>       <!-- 余额类型(BOOK:账面/AVAIL:可用) -->
  <Currency>CNY</Currency>
  <QueryTime>2026-03-24 14:30:00</QueryTime>
  <AccountStatus>ACTIVE</AccountStatus> <!-- 账户状态(ACTIVE/FROZEN等) -->
</BalanceResponse>
```

**使用规则** ⚠️:
```
调用频率限制: 1次/分钟 (防止频繁查询)
缓存策略: 查询结果缓存30秒(允许稍微延迟)
错误重试: 若查询失败,最多2次重试(间隔10秒)
         3次均失败 → 看板显示"余额查询失败",弹出重试按钮

调用时机:
1. 每日打款开始前(08:00) - 校验当日是否有足够资金
2. 提款申请时 - 实时校验可冻结金额
3. 资金不足告警 - 需要上游补充资金
4. 管理员手动刷新 - 限频1次/分钟
```

---

### 接口4: 批量转账 (100003)

**功能描述**: 批量付款文件上传提交，适用于结算日集中打款场景

**文件格式 - XML方案** (推荐):
```xml
<?xml version="1.0" encoding="GBK"?>
<BatchPay>
  <Header>
    <BatchNo>BATCH20260324001</BatchNo>        <!-- 批次号(唯一) -->
    <PaymentDate>20260324</PaymentDate>       <!-- 支付日期 -->
    <TotalPayAmount>500000.00</TotalPayAmount> <!-- 总金额 -->
    <TotalItem>5</TotalItem>                 <!-- 笔数 -->
    <CreatedTime>2026-03-24 08:00:00</CreatedTime>
  </Header>
  <Detail>
    <PayItem>
      <ItemNbr>001</ItemNbr>                 <!-- 项目号(1-n) -->
      <ReceiverAccount>9876543210</ReceiverAccount>
      <ReceiverName>合作方A公司</ReceiverName>
      <PayAmount>100000.00</PayAmount>
      <Reference>WD-001</Reference>          <!-- 对应业务单号 -->
      <Remark>3月份分成</Remark>
    </PayItem>
    <!-- 更多笔数... -->
  </Detail>
</BatchPay>
```

**调用时机**:
```
场景1: 结算日集中打款(推荐)
  - 每个自然月的结算日(如月底/月初)
  - 汇总当日所有审批通过的提款申请
  - 生成单个批量文件，一次性提交
  - 优势: 减少调用次数，提高效率

场景2(当前优先使用): 单笔转账(100001) 
  - 提款申请实时批准后立即单笔转账
  - 优势: 更灵活，错误处理单独可控
```

**返回结果**:
```xml
<BatchResponse>
  <BatchNo>BATCH20260324001</BatchNo>
  <ProcessingStatus>RCVD</ProcessingStatus>  <!-- RCVD:已收 / PRCS:处理中 -->
  <AcceptedTime>2026-03-24 09:00:00</AcceptedTime>
</BatchResponse>
```

---

### 接口5: 回单下载 (400001)

**功能描述**: 下载电子回单/凭证，作为打款凭证归档

**请求参数**:
```xml
<ReceiptDownload>
  <BankSerial>202603241234567890</BankSerial>  <!-- 银行流水号 -->
  <ReceiptType>ELECTRONIC</ReceiptType>        <!-- ELECTRONIC:电子版 / PAPER:纸质 -->
</ReceiptDownload>
```

**返回结果**:
- 返回PDF文件流 (application/pdf)
- 文件名: 回单_[银行流水号].pdf

**建议逻辑**:
```
1. 打款成功后，每隔5分钟尝试下载1次(最多5次)
2. 若5分钟内可用 → 立即下载
3. 若不可用 → 留给P1阶段的400002(回单查询)处理
4. 下载后存储到系统（数据库或对象存储）
5. 关联到提款申请单，支持合作方查看
6. 存储规范: 按年月日组织目录 /receipts/2026/03/24/回单_*.pdf
```

---

### 接口6: 流水查询 (300001)

**功能描述**: 下载交易流水明细，用于日终对账和合作方查询

**请求参数**:
```xml
<StatementQuery>
  <AccountNbr>1234567890</AccountNbr>
  <BeginDate>20260301</BeginDate>
  <EndDate>20260331</EndDate>
  <QueryType>ALL</QueryType>  <!-- ALL:全部 / DEBIT:付出 / CREDIT:收入 -->
</StatementQuery>
```

**返回参数 (样例)**:
```xml
<StatementList>
  <Total>125</Total>  <!-- 总笔数 -->
  <PageNo>1</PageNo>
  <PageSize>20</PageSize>
  <Transactions>
    <Transaction>
      <BankSerial>202603241234567890</BankSerial>
      <TradingDate>2026-03-24</TradingDate>
      <Amount>100000.00</Amount>
      <Direction>DEBIT</Direction>  <!-- DEBIT:付 / CREDIT:收 -->
      <Reference>WD-001</Reference>
      <RemitterName>公司名称</RemitterName>
      <ReceiverName>收款方名称</ReceiverName>
      <Status>SUCCESS</Status>
      <Remark>3月份分成</Remark>
    </Transaction>
    <!-- 更多交易... -->
  </Transactions>
</StatementList>
```

**对账处理**:
```
日终对账流程:
1. 查询当日银行流水(300001)
2. 遍历系统打款记录，与流水逐笔匹配:
   ✅ 匹配成功 → 状态一致，无异常
   ⚠️ 系统有但流水无 → 标记"待查证"(可能未到)
   ⚠️ 流水有但系统无 → 标记"异常交易"(数据缺失)
3. 差异自动归类，生成对账报告供财务人工处理
```

---

## 三、P1 重要接口清单(阶段2实现)

| # | 接口编码 | 接口名称 | 适用场景 | 备注 |
|---|--------|--------|--------|------|
| 7 | 100004 | 批量转账查询 | 结算日批量打款管理 | 仅与100003配套使用 |
| 8 | 500001 | 退票查询 | 异常处理→退票追踪 | **关键**: 原PRD未提及，但必需补充 |
| 9 | 500002 | 退票通知(推送) | 银行主动回调通知 | 可选(若银行支持推送) |
| 10 | 200002 | 日交易总额查询 | 每日打款前校验额度 | 与100001配套 |
| 11 | 300002 | 对账文件下载 | 日终自动对账 | 可选增强 |
| 12 | 400002 | 回单查询 | 回单下载失败→补救 | 查询生成状态 |

---

## 四、P2 建议接口清单(阶段3可选)

| # | 接口编码 | 接口名称 | 优先级 | 使用场景 | 备注 |
|----|--------|--------|------|--------|------|
| 13 | - | 手续费查询 | P2 | 预估打款成本 | 若规范包含(预留)；仅供财务查看 |
| 14 | - | 转账模板管理 | P2 | 重复打款场景 | 若规范包含(预留)；提高批量转账效率 |
| 15 | - | 收款人验证 | P2 | 打款前验证收款账户有效性 | 若规范包含(预留)；防止账户错误 |

---

## 五、关键重点与最佳实践

### 1. 最关键的接口流程

```
发起转账(100001) 
  ↓ [9:00-16:30 立即,其他时间排队]
等待ACPT状态
  ↓
轮询查询(100002) × 10次 [每30秒一次]
  ├─ SUCC(成功) → 下载回单(400001) → 对账(300001) → 完成
  ├─ RTRN(退票) → 释放冻结金额 → 通知财务+合作方
  ├─ RJCT(拒绝) → 标记失败 → 提示财务处理
  └─ PEND(处理中) → 继续轮询... → 超时后待人工处理
```

### 2. 三个必须的前置校验

打款前必须按顺序调用:

1. **(200001) 账户余额查询** → 校验 `余额 ≥ 转账金额`
2. **(200002) 日交易总额查询** → 校验 `已用额度 + 本次 ≤ 日限额`
3. **金额校验** → 若超单笔/日限 → 自动拆分为多笔转账

### 3. 退票处理流程(必加)

```
打款状态=RTRN(退票)
  ↓
识别退票原因(从状态码/表述)
  ├─ 账户名不匹配 → 合作方修改账户
  ├─ 账户不存在 → 合作方重新申请
  ├─ 余额不足 → 财务补充资金后重试
  └─ 其他原因 → 财务人工干预
  ↓
释放冻结金额回至"可提额"
  ↓
通知财务人员 + 通知合作方
  ↓
提供"修改后重新打款"选项
```

### 4. 超限自动拆分逻辑

```
检测到转账金额 > 单笔限额(500w)
  ↓
自动拆分为N笔
例如: 700w → 拆分为 [500w, 200w]
  ↓
逐笔调用100001(单笔转账)
  ↓
各笔独立轮询查询(100002)
```

---

## 六、业务流程与接口调用时序

### 提款全生命周期调用序列

```
1️⃣ 合作方发起提款申请【设定冻结金额】
   ├─ (100002) 单笔转账查询 → 检查是否有旧的未完成转账
   └─ 创建提款单,状态="审批中"

2️⃣ 业务负责人一审 + 财务终审【通过】
   └─ 提款单状态="已批准"，准备转账

3️⃣ 系统发起转账【建议时间段: 09:00-16:00】
   ├─ (200001) 账户余额查询 → 校验资金充足
   ├─ (200002) 账户日交易总额查询 → 校验日累计额度
   ├─ (100001) 单笔转账 → 发起转账
   │   返回: ACPT(接受) → 进入步骤4
   │   返回: RJCT(拒绝) → 标记失败,释放冻结,通知合作方
   └─ 提款单状态="打款中"

4️⃣ 轮询查询转账结果【间隔30秒,最多10次】
   ├─ (100002) 单笔转账查询 → BankSerial
   │   ├─ 返回SUCC → 步骤5
   │   ├─ 返回RTRN → 步骤6(退票)
   │   ├─ 返回RJCT → 步骤6(失败)
   │   └─ 返回ACPT/PEND → 继续轮询(最多10次)
   │       └─ 轮询超时 → 标记"待人工处理"

5️⃣ 打款成功【SUCC】
   ├─ (400001) 回单下载 → 获取电子凭证
   ├─ (300001) 流水查询 → 验证金额到账
   ├─ 扣减待结算金额(冻结→已结算)
   ├─ 提款单状态="已完成"
   └─ 通知合作方

6️⃣ 打款异常【RTRN/RJCT/超时】
   ├─ 记录异常原因
   ├─ 释放冻结金额 → 可提额
   ├─ 提款单状态="待人工处理"
   ├─ 通知财务人员干预
   └─ 提供"重新转账"或"撤回申请"选项

7️⃣ 日终对账【23:00执行】
   ├─ (300001) 流水查询 → 获取当日全部转账
   ├─ (300002) 对账文件下载 → 获取银行对账单(可选P1)
   ├─ (200001) 账户余额查询 → 验证账户余额
   └─ 自动匹配系统交易 vs 银行流水,生成对账报告
```

---

## 七、常见错误码及处理

| 错误类型 | 错误码 | 原因 | 系统处理 |
|---------|------|------|---------|
| 账户异常 | 1001 | 账户不存在/已禁用 | ❌ 标记失败，通知合作方修改银行信息 |
| 金额异常 | 1002 | 超单笔限额/日限额 | ❌ 标记失败，建议财务拆分转账 |
| 账户名异常 | 1003 | 收款户名与账号不匹配 | ❌ 銀行自动退票(RTRN)，通知合作方确认 |
| 手续费不足 | 1004 | 扣除手续费后资金不足 | ❌ 标记失败，提示资金不足 |
| 时间窗口外 | 1005 | 非工作时间/银行服务时间外 | ⏳ 排队至下一工作日08:30 |
| 金额格式错 | 1006 | 金额小数位错误 | ❌ 请求被拒，修正后重新提交 |
| 系统异常 | 9999 | 前置机通信异常/银行系统异常 | 🔄 自动重试(最多3次,间隔30s) |

---

## 八、实现路线图

### 阶段1(MVP) - 核心6个接口

**⏱️ 预计工期**: 3-4周

**交付物**:
- ✅ 单笔转账(100001) 完整实现
- ✅ 转账查询轮询(100002) + 状态机
- ✅ 余额查询(200001) + 缓存限频
- ✅ 回单下载(400001) + 定时重试
- ✅ 流水查询(300001) + 对账引擎
- ✅ 批量转账(100003) 或单笔替代
- ✅ 集成测试用例(正常/超限/退票场景)

### 阶段2(完整) - P1接口 + 退票补充

**⏱️ 预计工期**: 2-3周

**新增交付物**:
- ✅ 退票查询(500001) + 定时任务
- ✅ 日额度校验(200002)
- ✅ 对账文件下载(300002)
- ✅ 回单补救流程(400002)
- ✅ 异常处理完整化
- ✅ 财务看板增强

### 阶段3(增强) - P2可选接口

**⏱️ 预计工期**: 1-2周

- ✅ 手续费查询/预估
- ✅ 收款账户验证
- ✅ 转账模板管理

---

## 九、开发交付标准

### MVP阶段 (P0) 实现清单

- [ ] **接口100001(单笔转账)** 
  - [ ] 实现请求报文组装(XML格式)
  - [ ] 实现数字签名(RSA)
  - [ ] 处理超单笔限额的自动拆分逻辑
  - [ ] 单元测试(成功/失败/超限etc)

- [ ] **接口100002(转账查询)**
  - [ ] 实现轮询逻辑(30秒间隔,10次上限)
  - [ ] 状态机处理(ACPT/SUCC/RJCT/RTRN)
  - [ ] 超时告警机制

- [ ] **接口200001(余额查询)**
  - [ ] 实现缓存机制(30秒TTL)
  - [ ] 限频控制(1次/分钟)
  - [ ] 查询失败重试(2次)

- [ ] **接口100003(批量转账)** - 可先用单笔替代
  - [ ] XML批量文件生成器
  - [ ] 批量文件上传接口集成

- [ ] **接口400001(回单下载)**
  - [ ] 文件下载流实现
  - [ ] 定时轮询补救机制(5分钟内最多5次)
  - [ ] 本地存储逻辑

- [ ] **接口300001(流水查询)**
  - [ ] 分页查询实现
  - [ ] 对账匹配引擎(系统交易 vs 银行流水)
  - [ ] 对账报告生成

### 集成测试用例

1. **正常流程**: 发起转账 → 轮询成功 → 下载回单 → 对账通过
2. **超限场景**: 金额超单笔/日限 → 自动拆分/排队
3. **退票场景**: 账户名不匹配 → 自动识别RTRN → 释放冻结 → 通知合作方
4. **异常恢复**: 轮询超时 → 标记待处理 → 财务人工干预

---

## 十、待确认项

### 需要与银行/业务方确认

1. **接口编码确认** - 实际PDF规范中的接口编码是否与本清单一致?
2. **限额数值确认**:
   - 单笔转账限额: 暂定500万元
   - 日累计限额: 暂定5000万元
   - 是否需要其他限额维度?

3. **时间窗口确认**:
   - 批量转账受理时间窗口: 工作日08:30-16:30?
   - 是否包含节假日特殊处理?

4. **退票通知机制**:
   - 银行退票的回盘时效是多久?
   - 是否支持主动推送回调, 还是轮询查询?

5. **报文格式确认**:
   - 是否支持JSON格式, 还是仅支持XML?
   - 字段编码(GBK/UTF-8)?

---

## 附录A：相关需求文档关联

| 文档 | 版本 | 关键需求 |
|-----|-----|--------|
| PRD_大发行打款分账系统.md | V1.2 | REQ-F-203/208/209 等银企直联需求 |
| FDD_功能详细设计文档.md | V1.0 | 页面级功能设计与接口调用 |

---

**文档维护历史**:
- V1.0 (2026-04-01): 初版发布，梳理12个标准接口，完成PRD需求映射
