# 大发行结算平台 - 流水分成模式

**版本**：V1.0
**日期**：2026-04-23
**状态**：初稿

---

## 文档说明

本文档为大发行结算平台**流水分成模式**的产品需求文档（Phase 1）。

**流水分成模式**：以平台实际产生的投放流水为计算基础，系统自动从浑天仪按合作方平台渠道号拉取投放收入与投放成本，无需财务手工录入，公式简洁、数据来源清晰。

**利润分账模式**（Phase 2，后续规划）：在流水基础上叠加成本项、费用项、预留资金池等复杂扣减逻辑，适用于需要精细化成本核算的合作场景。

两个模式共用打款、BPM 审批、银企直联等基础能力，Phase 1 建设完成后可平滑扩展至 Phase 2。

---

## 一、背景与目标

### 1.1 背景

公司在大发行模式下，与多个合作方进行收入分账与打款结算。当前阶段，收入数据已可通过浑天仪平台按「应用ID + 渠道号」维度查询，具备自动化分账的数据基础。

原始方案中包含成本项手工录入、费用项月度账单拆分、预留资金池清算等复杂机制，取数依赖多，人工干预多。为优先落地核心结算能力，本期选择数据依赖最简的**流水分成模式**作为第一阶段建设目标。

### 1.2 目标

- 以合作方在平台（微信小游戏 / 抖音小游戏）下的渠道号为取数依据，自动拉取投放收入与投放成本，无需财务手工录入
- 按 `合作方分账金额 = （投放收入 - 投放成本）× 分成比例` 完成自动化结算计算
- 支持合作方自主发起打款申请，走 BPM 审批流，银企直联自动打款，形成完整结算闭环

### 1.3 不在本期范围内

| 功能 | 原因 | 规划阶段 |
|---|---|---|
| 成本项 / 费用项手工录入 | 取数来源较复杂，先验证流水分成闭环 | Phase 2 |
| 费用录入待办机制 | 依赖费用项配置 | Phase 2 |
| 预留资金池与合作期末清算 | 依赖费用项 | Phase 2 |
| 阶梯式分成 | 非当前核心诉求 | Backlog |

---

## 二、核心概念

### 2.1 分成公式

```
合作方分账金额 = （合作方投放收入 - 合作方投放成本）× 分成比例

本期转入待结算 = 合作方分账金额
```

> **合作方投放收入**：浑天仪按「平台方应用ID + 平台方渠道号」拉取当期投放收入流水，系统只读，不支持修改。
>
> **合作方投放成本**：浑天仪按「平台方应用ID + 平台方渠道号」拉取当期投放成本流水，系统只读，不支持修改。
>
> **分成比例**：分成规则中配置，审批通过后不可修改。

### 2.2 取数逻辑

每个合作方在特定游戏、特定平台下，有唯一的**平台方渠道号**，与微信 / 抖音后台「游戏应用 → 渠道管理」中的渠道号一一对应。

系统从浑天仪拉取数据时，使用以下两个参数精确定位数据源：

| 参数 | 数据来源 |
|---|---|
| 平台方应用ID | 游戏管理 → 上架平台信息中配置，与平台后台应用ID保持一致 |
| 平台方渠道号 | 合作方-游戏-平台关系中配置，与平台后台该游戏下的渠道号保持一致 |

### 2.3 结算维度

结算以 **合作方 + 游戏 + 平台类型** 为独立核算单元。

同一合作方可能在微信和抖音分别推广同一款游戏，两个平台分别独立计算分账金额并出账。合作方发起打款时，可选择合并或分批提交申请。

---

## 三、角色与权限

### 3.1 角色定义

| 角色 | 描述 |
|---|---|
| **系统管理员** | 维护游戏字典（含上架平台信息）、分配用户权限 |
| **业务负责人** | 配置合作方-游戏-平台关系（含平台方渠道号） |
| **财务人员** | 维护合作方主体与银行账户、查看分账明细、处理退票/失败异常 |
| **财务负责人/审批人** | 审批分成规则配置单、审批打款申请单 |
| **合作方** | 查看本方结算数据与打款记录、发起打款申请 |

### 3.2 权限矩阵

| 功能模块 | 合作方 | 财务人员 | 财务负责人/审批人 | 业务负责人 | 系统管理员 |
|---|---|---|---|---|---|
| 游戏管理 | 只读 | 只读 | 只读 | 只读 | 创建/编辑/停用 |
| 合作方主体管理 | 查看本方 | 创建/编辑 | 查看 | 查看 | 查看 |
| 银行账户管理 | 查看本方 | 创建/编辑 | 查看 | 查看 | 查看 |
| 合作方-游戏-平台关系 | 无 | 只读 | 只读 | 创建/编辑（权限范围内） | 全量查看 |
| 分成规则配置 | 无 | 创建/提交审批 | 审批 | 审批/查看 | 只读 |
| 分账明细查看 | 查看本方 | 查看全量 | 查看全量 | 查看权限范围 | 全量查看 |
| 打款申请发起 | 发起本方申请 | 代发/查看 | 无 | 无 | 无 |
| 打款审批 | 无 | 无 | 审批 | 按流程参与 | 无 |
| 打款结果查看 | 查看本方 | 查看全量 | 查看全量 | 查看权限范围 | 全量查看 |
| 退票/失败处理 | 查看结果 | 处理 | 查看/审批 | 查看 | 查看 |

---

## 四、功能模块详细设计

### 4.1 模块一：游戏管理

#### 4.1.1 模块定位

游戏是结算系统的基础字典，由系统管理员统一维护。每个游戏支持配置多个上架平台，平台方应用ID与微信 / 抖音后台的应用ID保持一致，是系统从浑天仪拉取数据的关键标识。

#### 4.1.2 游戏基础字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 游戏ID | String | 系统生成 | 全局唯一 |
| 游戏名称 | String | 是 | 游戏全称 |
| 游戏编码 | String | 是 | 系统内唯一标识，创建后不可修改 |
| 状态 | Enum | 系统生成 | 启用 / 停用 |
| 备注 | String | 否 | 可选说明 |
| 创建人 | Reference | 系统生成 | 系统管理员 |
| 创建时间 | DateTime | 系统生成 | — |

#### 4.1.3 上架平台字段（子表，一个游戏可配置多条）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 记录ID | String | 系统生成 | — |
| 所属游戏 | Reference | 系统生成 | 关联游戏 |
| 平台类型 | Enum | 是 | 微信小游戏 / 抖音小游戏 |
| 平台方应用ID | String | 是 | 与平台后台应用ID保持一致，手动填写 |
| 备注 | String | 否 | 可选说明 |
| 创建人 | Reference | 系统生成 | 系统管理员 |
| 创建时间 | DateTime | 系统生成 | — |

#### 4.1.4 功能需求

- **REQ-G-101**：系统应支持系统管理员新增、编辑游戏基础信息；游戏编码创建后不可修改，修改时字段禁用。
- **REQ-G-102**：系统应支持在游戏下新增、编辑上架平台信息；同一游戏下同一平台类型只允许一条记录（唯一性约束），重复时拒绝保存并提示。
- **REQ-G-103**：当游戏被停用时，系统应禁止在新建分成规则及合作方-游戏-平台关系中引用该游戏；已有数据不受影响。
- **REQ-G-104**：游戏列表及上架平台信息对系统管理员以外的角色只读，不显示编辑入口。
- **REQ-G-105**：平台方应用ID修改后，后续结算取数将使用新ID；系统应在编辑时提示"修改后将影响后续结算取数，历史已完成结算数据不受影响"。

---

### 4.2 模块二：合作方主体管理

#### 4.2.1 模块定位

合作方主体由财务统一维护，作为分成规则、银行账户、打款申请等模块的基础主实体。主体档案创建后即可被业务负责人配置游戏关系，配置关系后不可删除。

#### 4.2.2 主体档案字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 主体ID | String | 系统生成 | 全局唯一 |
| 公司名称 | String | 是 | 合作方公司全称，与营业执照一致 |
| 统一社会信用代码 | String | 是 | 全局唯一，不可重复 |
| 营业执照 | File | 是 | 扫描件或加盖公章复印件，支持 JPG/PNG/PDF，≤ 10MB |
| 法人姓名 | String | 是 | 法定代表人 |
| 法人证件号 | String | 是 | 加密存储，展示时脱敏 |
| 联系人姓名 | String | 是 | 日常对接人 |
| 联系人手机号 | String | 是 | — |
| 联系人邮箱 | String | 否 | — |
| 合作状态 | Enum | 系统生成 | 合作中（本期只支持该状态） |
| 创建人 | Reference | 系统生成 | 财务人员 |
| 创建时间 | DateTime | 系统生成 | — |
| 最后修改人 | Reference | 系统生成 | — |
| 最后修改时间 | DateTime | 系统生成 | — |

#### 4.2.3 银行账户字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 账户ID | String | 系统生成 | — |
| 所属合作方主体 | Reference | 系统生成 | 1:1 关联 |
| 账户名称 | String | 是 | 收款账户名称 |
| 开户银行 | String | 是 | — |
| 开户支行 | String | 是 | — |
| 银行账号 | String | 是 | 加密存储，展示时脱敏 |
| 联行号 | String | 是 | 跨行转账使用 |
| 账户用途 | String | 否 | 备注说明 |
| 创建人 | Reference | 系统生成 | 财务人员 |
| 创建时间 | DateTime | 系统生成 | — |
| 最后修改人 | Reference | 系统生成 | — |
| 最后修改时间 | DateTime | 系统生成 | — |

#### 4.2.4 功能需求

- **REQ-P-101**：系统应支持财务人员创建合作方主体档案；统一社会信用代码全局唯一，重复时拒绝提交。
- **REQ-P-102**：系统应支持财务人员为合作方主体新增/编辑银行账户，每个合作方有且仅有一个银行账户；保存后立即生效，不走审批流程；编辑操作覆盖原账户信息，变更前后完整信息记入操作日志。
- **REQ-P-103**：当合作方主体尚未配置任何游戏关系时，允许财务删除该主体（同步删除关联银行账户）；已配置游戏关系时禁止删除，提示"该合作方已绑定游戏关系，无法删除"。
- **REQ-P-104**：合作方主体与银行账户创建或更新后，系统应自动同步至银企直联系统；银企直联系统中的同步数据标记为"只读来源数据"，不允许反向修改。
- **REQ-P-105**：发起打款申请前，系统应校验收款账户信息完整性（账户名称、开户银行、开户支行、银行账号、联行号均不为空），不完整时拒绝提交并提示完善账户信息。
- **REQ-P-106**：打款申请提交时，系统应快照当时完整账户信息至申请单，后续账户变更不影响历史打款记录的展示与追溯。

---

### 4.3 模块三：合作方-游戏-平台关系管理

#### 4.3.1 模块定位

由业务负责人配置，定义某合作方在某游戏的某平台（微信 / 抖音）下对应的**平台方渠道号**。

这条关系是分账计算引擎的核心取数依据：系统通过「平台方应用ID + 平台方渠道号」从浑天仪精确拉取该合作方的投放收入与投放成本数据。

#### 4.3.2 关系字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 关系ID | String | 系统生成 | — |
| 合作方主体 | Reference | 是 | 从已启用合作方列表选取 |
| 游戏 | Reference | 是 | 从已启用游戏列表选取 |
| 平台类型 | Enum | 是 | 微信小游戏 / 抖音小游戏 |
| 平台方渠道号 | String | 是 | 该合作方在此平台+此游戏下的渠道号，与平台后台保持一致，手动填写 |
| 业务负责人 | Reference | 是 | 配置者，作为该关系的数据权限归属 |
| 状态 | Enum | 系统生成 | 启用 / 停用 |
| 创建人 | Reference | 系统生成 | 业务负责人 |
| 创建时间 | DateTime | 系统生成 | — |
| 最后修改人 | Reference | 系统生成 | — |
| 最后修改时间 | DateTime | 系统生成 | — |

#### 4.3.3 功能需求

- **REQ-R-101**：系统应支持业务负责人创建合作方-游戏-平台关系；创建时需选择合作方、游戏、平台类型，并填写平台方渠道号。
- **REQ-R-102**：同一「合作方 + 游戏 + 平台类型」组合只能存在一条启用状态的关系（唯一性约束）；重复时拒绝提交并提示。
- **REQ-R-103**：关系创建后，系统将其作为分账计算引擎的取数依据；后续浑天仪数据同步将按该关系中的「平台方应用ID + 平台方渠道号」进行匹配。
- **REQ-R-104**：当某关系存在生效中的分成规则时，禁止停用该关系；需等关联规则全部到期后方可停用。若需变更渠道号或合作方，需停用旧关系并创建新关系。
- **REQ-R-105**：业务负责人的数据权限范围由其配置的关系决定，只能查看和管理自己创建的关系。
- **REQ-R-106**：关系与分成规则、分账明细、打款申请单均保留历史关联，不因关系停用而清除。

---

### 4.4 模块四：分成规则配置

#### 4.4.1 规则模型

每条规则绑定「合作方 + 游戏 + 平台类型」，审批通过后内容不可修改。如需调整，需创建新规则并设置不重叠的时间区间。

#### 4.4.2 规则字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 规则ID | String | 系统生成 | — |
| 合作方主体 | Reference | 是 | — |
| 游戏 | Reference | 是 | — |
| 平台类型 | Enum | 是 | 微信小游戏 / 抖音小游戏 |
| 分成比例 | Decimal | 是 | 合作方分成百分比，如 70% |
| 生效开始时间 | Date | 是 | 财务手动填写 |
| 生效结束时间 | Date | 是 | 财务手动填写 |
| 结算周期 | Enum | 是 | 按月 / 按周 |
| 备注 | String | 否 | 可选说明 |
| 规则状态 | Enum | 系统生成 | 草稿 / 审批中 / 生效中 / 已到期 / 已驳回 |
| BPM 实例号 | String | 系统生成 | BPM 发起审批后回写 |
| 审批通过时间 | DateTime | 系统生成 | BPM 通过时自动写入 |
| 创建人 | Reference | 系统生成 | 财务人员 |
| 创建时间 | DateTime | 系统生成 | — |

#### 4.4.3 功能需求

- **REQ-RU-101**：系统应仅允许财务人员创建分成规则；创建前需确认已存在对应的合作方-游戏-平台关系，否则提示先配置关系。
- **REQ-RU-102**：规则审批通过后，所有字段不可修改，系统应在界面上禁用编辑入口；如需调整，须重新创建新规则。
- **REQ-RU-103**：提交时系统自动校验（同时满足以下三项方允许提交）：
  - 结束时间 > 开始时间
  - 所填时间区间内，该「合作方 + 游戏 + 平台类型」维度无已出账记录
  - 所填时间区间与该维度已审批通过的其他规则无重叠
- **REQ-RU-104**：当财务提交规则时，系统应触发 BPM 审批流程；审批通过后规则状态变更为"生效中"，所有字段锁定。
- **REQ-RU-105**：规则被驳回后标记"已驳回"，不生效；财务可重新创建新规则提交审批。
- **REQ-RU-106**：规则无手动停用操作，以生效结束时间自然到期，状态自动变更为"已到期"。
- **REQ-RU-107**：系统应支持从已有规则复制创建新规则，复制后各字段可编辑。
- **REQ-RU-108**：系统应在规则详情中展示审批状态、审批通过时间、生效开始/结束时间及完整分成公式说明。

---

### 4.5 模块五：分账计算引擎

#### 4.5.1 总体流程

```
【每日收入同步阶段】
系统每日 T+1 从浑天仪同步投放收入与投放成本数据
  ↓
按「平台方应用ID + 平台方渠道号」匹配合作方-游戏-平台关系
  ↓
按「合作方 + 游戏 + 平台类型」维度累加写入收入池与成本池
  ↓
若无匹配关系：跳过并生成告警通知业务负责人

【账期结算阶段】
结算账期由规则自动推算（从生效开始时间按结算周期步进）
账期到期时，系统自动触发结算，无需财务手动操作
  ↓
系统读取本期收入池（投放收入）与成本池（投放成本）数据（只读）
  ↓
按公式计算：
  合作方分账金额 = （投放收入 - 投放成本）× 分成比例
  本期转入待结算 = 合作方分账金额
  ↓
校验：分账金额 < 0 → 告警通知财务，允许负值写入
  ↓
结算结果写入分账明细，更新虚拟账户待结算金额
```

#### 4.5.2 收入池与成本池数据结构

| 字段 | 类型 | 说明 |
|---|---|---|
| 记录ID | String | 系统自动生成 |
| 合作方主体 | Reference | — |
| 游戏 | Reference | — |
| 平台类型 | Enum | 微信小游戏 / 抖音小游戏 |
| 数据日期 | Date | T+1 同步时归入 T 日 |
| 结算账期 | Date Range | 该数据所属结算周期 |
| 投放收入 | Decimal | 浑天仪同步，只读，不可修改 |
| 投放成本 | Decimal | 浑天仪同步，只读，不可修改 |
| 同步时间 | DateTime | 系统从浑天仪拉取数据的时间 |
| 数据来源 | Enum | 自动同步 / 补录 |

#### 4.5.3 虚拟账户数据结构

以「合作方 + 游戏 + 平台类型 + 规则」为维度独立核算：

| 字段 | 说明 |
|---|---|
| 待结算金额 | 历次结算累计转入，扣除已打款后余额 |
| 冻结金额 | 打款申请提交后至完成前冻结的金额 |
| 可打款金额 | = 待结算金额 - 冻结金额（实时计算，每次变更时同步写回） |
| 已结算金额 | 历次打款成功的累计金额 |

**金额流转规则：**

```
待结算金额 += 本期转入待结算          ← 结算成功后
待结算金额 -= 每笔打款成功金额        ← 打款成功后
冻结金额   += 打款申请提交金额        ← 申请提交时
冻结金额   -= 打款审批驳回金额        ← 审批驳回时
冻结金额   -= 打款成功/失败/退票金额  ← 各终态处理完成时
已结算金额 += 每笔打款成功金额        ← 打款成功后

可打款金额  = 待结算金额 - 冻结金额  ← 每次变更时重算写回
```

#### 4.5.4 功能需求

- **REQ-E-101**：系统应每日 T+1 从浑天仪同步投放收入与成本数据，按「平台方应用ID + 平台方渠道号」匹配合作方关系，写入对应维度的收入池与成本池；同步失败时告警通知财务，不触发当日相关结算。
- **REQ-E-102**：结算账期到期时，系统应自动触发分账计算，无需财务手动操作；系统对同一结算账期保证幂等性，不允许重复结算。
- **REQ-E-103**：当账期内未匹配到生效中规则时，系统应阻断结算并告警通知财务，不写入待结算；所有阻断情形均不自动重试。
- **REQ-E-104**：当账期内无对应合作方-游戏-平台关系时，系统应阻断结算并告警通知业务负责人补充配置。
- **REQ-E-105**：当 `合作方分账金额 < 0`（成本超过收入）时，系统不阻断结算，允许负值写入待结算，告警通知财务核查录入数据。
- **REQ-E-106**：补录数据按实际产生日期归入对应账期收入池；若对应账期已完成结算，系统告警提示财务确认是否重新触发结算，不自动重算。

#### 4.5.5 异常处理策略

| 异常情形 | 处理策略 | 通知对象 |
|---|---|---|
| 浑天仪数据同步失败 | 告警，不触发当日结算，等待人工确认后补录 | 财务 |
| 账期内无匹配合作方关系 | 阻断结算，告警 | 业务负责人 |
| 账期内无生效中规则 | 阻断结算，告警 | 财务 |
| 分账金额 < 0 | 不阻断，负值写入，告警 | 财务 |

---

### 4.6 模块六：分账明细查看

#### 4.6.1 分账明细记录字段

| 字段 | 说明 |
|---|---|
| 明细ID | 系统自动生成 |
| 合作方主体 | — |
| 游戏 | — |
| 平台类型 | 微信小游戏 / 抖音小游戏 |
| 结算账期 | 对应账期范围 |
| 关联规则ID | 使用的分成规则 |
| 本期投放收入 | 浑天仪同步，只读 |
| 本期投放成本 | 浑天仪同步，只读 |
| 分成比例 | 规则中配置 |
| 本期分账金额 | =（投放收入 - 投放成本）× 分成比例 |
| 本期转入待结算 | = 本期分账金额 |
| 结算触发时间 | 系统自动触发时间 |
| 触发方式 | 系统自动触发 / 财务手动重算 |

#### 4.6.2 功能需求

- **REQ-D-101**：系统应支持按「合作方 + 游戏 + 平台类型 + 账期」维度查看分账明细，支持按合作方、游戏、平台类型、时间范围筛选。
- **REQ-D-102**：分账明细数据只读，不可修改。
- **REQ-D-103**：当分账金额为负值时，系统应在明细中以红色标注并展示提示说明。
- **REQ-D-104**：系统应支持导出分账明细数据（Excel 格式）。
- **REQ-D-105**：分账明细应支持按合作方、游戏、平台汇总展示，财务可查看各维度待结算金额汇总。

---

### 4.7 模块七：合作方自主发起打款

#### 4.7.1 流程说明

```
合作方登录系统，选择游戏 + 平台类型，填写打款金额与备注
系统自动关联合作方主体及收款银行账户（只读展示）
  ↓
系统校验：
  ① 打款金额 ≤ 该维度下的可打款金额
  ② 合作方主体状态为"合作中"
  ③ 收款账户信息完整（账户名称/银行/支行/账号/联行号均不为空）
  ↓
校验通过 → 冻结对应金额
  → 同步校验资金池余额（不足时通知资金组，不阻断流程）
  ↓
生成打款申请单（快照完整账户信息）→ 发起 BPM 审批
  ↓
审批通过 → 校验资金池余额
  → 不足则标记"排队中"，等余额充足后自动执行
  → 充足则调用银企直联系统发起转账
  ↓
银行回调银企直联 → 银企直联回调大发行系统
  ↓
成功：冻结→已结算，归档电子回单
失败：冻结自动释放，通知财务，展示"重新打款"按钮
退票：状态→"已退票-待财务确认"，财务核查后手动解冻
```

#### 4.7.2 申请单字段

| 字段 | 类型 | 说明 |
|---|---|---|
| 申请单号 | String | 系统自动生成 |
| 游戏 | Reference | 合作方选择 |
| 平台类型 | Enum | 合作方选择（微信小游戏 / 抖音小游戏） |
| 合作方主体 | Reference | 系统自动关联，只读 |
| 收款账户名称 | String | 快照：提交时从银行账户自动抓取 |
| 开户银行 | String | 快照 |
| 开户支行 | String | 快照 |
| 银行账号 | String | 快照，加密存储 |
| 联行号 | String | 快照 |
| 打款金额 | Decimal | 合作方手动填写，≤ 可打款金额 |
| 申请备注 | String | 业务说明，可选 |
| 发起人 | Reference | 合作方用户（代发时记录实际操作财务及代发原因） |
| 发起时间 | DateTime | — |
| 审批状态 | Enum | 待审批 / 已通过 / 已驳回 |
| 执行状态 | Enum | 待执行 / 排队中 / 打款中 / 已完成 / 转账失败 / 已退票-待财务确认 / 已退票-已解冻 / 无结果 |
| BPM 实例号 | String | BPM 系统返回 |
| 银企资金流水号 | String | 银企直联执行完成后返回并关联 |
| 银行交易流水号 | String | 银行返回 |
| 电子回单 | File | 成功后自动下载归档 |

#### 4.7.3 失败与退票处理

**转账失败**（资金从未出账）：
- 申请单状态 → "转账失败"
- 冻结金额**自动释放**回可结算金额
- 通知财务与合作方
- 申请单详情展示"重新打款"按钮，由合作方手动触发，需走完整申请与审批流程

**退票**（跨行转账已成功出账，接收方银行退回）：
- 银企直联主动轮询退单接口，将退回流水与原转出流水建立关联
- 申请单状态 → "已退票-待财务确认"，资金状态维持"已结算"不变，不自动回滚
- 通知财务：退票原因、原单号、退回流水单号，提示核查收款账户信息
- 财务核查确认后手动触发解冻；解冻完成后：
  - 已结算金额回滚为可结算金额
  - 申请单状态 → "已退票-已解冻"
  - 展示"重新打款"按钮，合作方手动触发，需走完整流程

---

### 4.8 模块八：BPM 审批流

- 分成规则配置审批与打款申请审批均通过 BPM 系统完成，大发行结算平台只读展示审批状态与结果
- 系统自动发起 BPM 流程，回写 BPM 实例号、当前审批节点与审批状态
- 审批通过后系统自动推进业务状态；审批驳回后系统自动结束当前业务单并执行冻结释放逻辑
- **BPM 就绪是系统上线的前提条件**，不提供手工维护审批状态的兜底能力

---

## 五、通知机制

### 5.1 通知渠道

所有系统通知通过以下两个渠道同步推送：

1. **飞书消息**：向对应角色绑定的飞书账号推送
2. **系统个人中心**：登录后可查看未读通知

### 5.2 通知场景

| 通知场景 | 接收角色 | 通知内容 |
|---|---|---|
| 浑天仪数据同步失败 | 财务 | 同步失败日期、影响维度，提示不触发结算 |
| 结算阻断（无匹配关系） | 业务负责人 | 阻断维度，提示补充配置合作方-游戏-平台关系 |
| 结算阻断（无生效规则） | 财务 | 阻断维度、账期，提示核查规则生效时间 |
| 分账金额 < 0 | 财务 | 维度、账期、当期投放收入/成本/分账金额明细 |
| 资金池余额不足（申请提交时） | 资金组 | 不足金额，提示补充资金，不阻断流程 |
| 资金池余额不足（打款排队中） | 资金组 | 排队中申请数量、缺口金额 |
| 打款成功 | 财务、合作方 | 申请单号、打款金额、资金流水号 |
| 打款失败 | 财务、合作方 | 申请单号、失败原因，提示手动处理 |
| 打款退票 | 财务 | 申请单号、退票原因，提示核查收款账户信息 |
| 补录数据影响已结算账期 | 财务 | 账期、补录金额，提示确认是否重新触发结算 |

---

## 六、非功能需求

| 类型 | 要求 |
|---|---|
| 性能 | 核心列表页首屏加载 ≤ 2s；分账明细查询（百万级数据）≤ 3s |
| 安全 | 基于 RBAC 模型进行权限控制；银行账号、法人证件号加密存储，展示时脱敏 |
| 可用性 | 年度可用性 ≥ 99.9%；RPO ≤ 1h，RTO ≤ 4h |
| 审计 | 规则变更、审批操作、打款执行等关键动作保留完整操作日志，含操作人、时间、变更前后数据 |
| 幂等 | 同一结算账期不允许重复结算；金额冻结/释放/变更采用并发控制机制保障一致性 |
| 主数据同步 | 合作方主体与银行账户从大发行系统单向同步至银企直联系统，银企直联系统不得反向修改 |

---

## 七、两个系统协同说明

### 7.1 系统职责边界

**大发行结算平台**（本系统）：
- 合作方主体管理、银行账户管理
- 游戏与上架平台信息管理
- 合作方-游戏-平台关系管理
- 分成规则配置与审批
- T+1 分账计算
- 可结算金额、冻结金额、已结算金额管理
- 打款申请单管理与状态流转
- BPM 审批流转
- 手动解冻操作
- 业务审计与数据看板

**银企直联系统**：
- 对接银行银企直联接口
- 接收合作方主体与银行账户同步数据（只读使用）
- 生成资金流水单号、发起银行转账
- 接收银行回调，标准化结果回传至大发行系统
- 提供余额查询、流水查询、转账日志查询能力

### 7.2 主数据同步

```
大发行结算平台（主数据源）
  ├─ 创建/维护 合作方主体
  └─ 创建/维护 合作方银行账户
              ↓ 自动同步（单向）
       银企直联系统（只读使用）
  ├─ 生成收款对象主数据
  └─ 仅允许查询与使用，不允许修改
```

---

## 八、待确认事项

1. **结算维度粒度**：当前设计为「合作方 + 游戏 + 平台类型」分别独立出账。若同一合作方在微信和抖音均有合作，是否需要支持合并为一笔打款申请？还是保持分平台独立发起？

2. **浑天仪接口规范**：按「平台方应用ID + 平台方渠道号」拉取投放收入与投放成本的接口协议、字段命名、数据延迟（T+1 的具体时效）需与浑天仪团队确认。

3. **负值结算处理**：分账金额为负时允许写入。下一期是否支持负值自动在下期冲抵？还是由财务手动确认处理？

4. **重新触发结算**：补录数据影响已结算账期时，财务确认后重新触发结算的操作权限与流程（是否需要二次审批或留痕）？

5. **BPM 就绪时间**：BPM 审批流为强依赖，需提前确认 BPM 上线时间节点，避免阻塞打款与规则配置核心功能交付。

6. **多笔排队打款顺序**：当资金池余额不足时，多笔排队申请按审批通过时间顺序执行，是否有人工干预优先级的诉求？

---

## 九、术语表

| 术语 | 说明 |
|---|---|
| 流水分成 | Phase 1 分成模式：以平台投放流水为计算基础，数据自动取数，无手工录入 |
| 利润分账 | Phase 2 分成模式：在流水基础上叠加成本项、费用项等复杂扣减逻辑 |
| 平台方应用ID | 游戏在微信/抖音平台的唯一标识，与平台后台保持一致 |
| 平台方渠道号 | 合作方在特定平台+特定游戏下的渠道唯一标识，与平台后台渠道管理中的渠道号一一对应 |
| 浑天仪 | 公司内部数据平台，提供按应用ID+渠道号维度查询投放收入与成本数据的能力 |
| 银企直联 | 银行与企业之间的直连系统，提供转账、余额查询、流水查询等底层资金能力 |
| 待结算金额 | 已完成分账计算但尚未打款的累计金额 |
| 冻结金额 | 打款申请提交后至完成前暂时冻结的金额 |
| 可打款金额 | = 待结算金额 - 冻结金额，打款申请金额不得超过此值 |
| T+1 | 数据产生后第二个工作日同步，如 4月22日数据于4月23日同步入库 |
